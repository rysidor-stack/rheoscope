# Confidence audit (fixture)

Fixture content only -- present so the confidence-audit check does not add noise;
this tree's single intended violation class is the post-protocol absent
locked_by_raw_file key.
