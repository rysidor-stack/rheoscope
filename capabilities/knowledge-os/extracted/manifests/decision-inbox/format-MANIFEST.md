---
manifest: format
surface: decision-inbox
version: "1.2"
status: EXTRACTED
source_artifacts:
  - path: deploy/decision-inbox.py
    sha256: 3c3882247d408006d4cef05736026b464c7264fafac08ba0808a17564e9555fd
extracted: 2026-07-23
amended: 2026-08-09 (A3)
toolchain: source-read
confidence: source-crosschecked
row_shape: table
declared_rows: 13
schema_extensions: []
---

# decision-inbox format manifest

Format-layer behavioral contract for `DECISIONS-PENDING.md`, the derived projection
`deploy/decision-inbox.py` regenerates. Session D, surface 2 of the harness-surfaces
dogfood (the session-D design brief (dev-repo record, not shipped) Part 1). Every row
below is a **structural assertion a future `deploy/check-briefing-format.py`-style
validator would implement** against a DECISIONS-PENDING.md instance — this manifest
enumerates the contract honestly at EXTRACTED; no validator and no pinned fixture seed
are owed at this stage (only surface 1, `sweep-briefing`, is required to reach
CERTIFIED in session D). Every row carries the `DRAFT` flag: the row-lifecycle carrier
(manifest-format.md §5), never a quality judgment — these rows are unpinned assertions
until a certification round graduates them via a named amendment.

The contract was derived entirely by reading the producer, `deploy/decision-inbox.py`
(pinned above by sha256) — its `_SECTIONS` tuple, its per-source regex/format
constants, and its embedded `self_test()` fixtures, which double as the plainest
statement of intended behavior in the file. The live `DECISIONS-PENDING.md` at the
fork root was read for cross-reference only (its content is a moving target, never a
source_artifacts pin — the projection is regenerated, so pinning an instance would
immediately go stale; the producer code is the stable contract).

**Extraction note — the producer was mid-flight under a concurrent session.**
`deploy/decision-inbox.py` carried an uncommitted, in-progress working-tree change
while this manifest was being authored (`git diff --stat` showed +348/-17 against
HEAD): the v3.0-39 item 2 "inbox ages" feature (age-tail rendering backed by
`receipts/desk/inbox-first-seen.json`) landing live, presumably from a sibling
session-D builder on the same design brief's Part 2. The sha256 above was re-pinned
against the LAST-OBSERVED snapshot (self-test cases through at least (25) present,
covering first-generation stamping, regeneration, retirement, revival, and corrupt-
sidecar tolerance) rather than the pre-feature snapshot this extraction started
against — the more complete, better-tested contract, and the one that will actually
ship. **If that other session commits further changes to this file, this manifest's
`source_artifacts` pin goes stale immediately** and needs a same-commit amendment
(manifest-format.md §8) the next time anyone touches this surface — re-run
`python deploy/check-manifest.py` before treating this manifest as settled.

## Rows

| id | name | replay path | expected observable | variant | flags | evidence | kind |
|---|---|---|---|---|---|---|---|
| `di-header-comment` | Header line is the derived-projection HTML comment | Parse line 1 of the file | Line 1 equals `<!-- DERIVED PROJECTION -- regenerated by deploy/decision-inbox.py; never hand-edit; the sources win. Regenerated: <date>. -->` where `<date>` is an ISO `YYYY-MM-DD` | — | DRAFT | source-read | EXACT |
| `di-all-clear-exact-body` | Zero-item body is the exact all-clear projection, nothing else | Read the full body when every source (candidate/backlog/sweep/flight_plan/marker) yields zero items | Body equals the header line, one blank line, then the single literal line `Nothing is waiting on you. All clear.`, then a trailing newline — no `# Waiting on you` heading and no `##` section heading anywhere in the file | — | DRAFT | source-read | EXACT |
| `di-waiting-heading-gated` | `# Waiting on you` heading present iff at least one item exists | Count items across all five sections; check for the heading | Heading present exactly when the total item count is > 0; absent exactly when it is 0 — mutually exclusive with the all-clear sentence, never both, never neither | — | DRAFT | source-read | EXACT |
| `di-section-order` | Present sections render in the fixed source order | List the `##` headings present in a multi-source fixture | Any subset of {`Session candidates awaiting your confirmation`, `Backlog items awaiting a decision`, `From the last sweep`, `Flight-plan blockers`, `Flagged in a document`, `Fixes the system can do on your yes (from the last sweep)`} that is present appears in exactly that left-to-right order (A2: the consented-fix section renders LAST — chores never crowd the decisions); a source with zero items contributes no heading at all (never an empty section) | — | DRAFT | source-read | EXACT |
| `di-candidate-section-first` | Session-candidates section, when present, always leads | In a fixture where the candidate source and at least one other source both have items | `## Session candidates awaiting your confirmation` is the first `##` heading in the file, ahead of every other present section (R-1 build-decisions Part 7: the operator's "yes" here starts something, so it leads) | — | DRAFT | source-read | EXACT |
| `di-item-line-shape` | Every item line is a plain dashed line with an age tail then a details tail | Match every line rendered under a `##` section heading | Line matches `- <text><age-tail> *(details: <path-or-token>)*` — a dash and space (A2: the former `- [ ]` checkbox glyph was an affordance lie — ticking did nothing and regeneration erased it; no checkbox renders anywhere), `<text>`, then `<age-tail>` immediately appended (no extra space — the tail itself opens with one), then a space, then `*(details: <path-or-token>)*` | — | DRAFT | source-read | EXACT |
| `di-action-channel-line` | The real yes-channel is stated once, right under the title | Read the two lines after `# Waiting on you` when at least one item exists | A single plain-prose line stating the file is a read-only view and that acting on an item means SAYING so in any session ("yes to" plus the item's id, or quoting the item), and that edits here do nothing; renders exactly once, only when the waiting heading renders (A2, plain-language sweep Q2) | — | DRAFT | source-read | EXACT |
| `di-age-tail-and-identity` | Age tail wording and the per-source stable-identity key it is computed from | Read `receipts/desk/inbox-first-seen.json` for the item's identity key, then compare its `first_seen` date against the generation date | `<age-tail>` is exactly `" — new today"` (em dash) when the identity has no prior record or `first_seen` equals the generation date, else `" — waiting N days"` (`"day"` singular only at N=1); identity keys are stable across cosmetic/timestamp/rewording churn — `candidate:<candidate_id>`, `backlog:<entry-id>`, `sweep:<hash of the NORMALIZED full prose — case- and punctuation-folded, never the date suffix>` (A2: a wording-fragile hash silently reset ages, the inbox's one honesty feature), OR `backlog:<id>` inherited when the sweep prose names a backlog entry id — in which case the sweep telling is SUPPRESSED if that backlog item already renders (cross-source dedup, first section wins) — plus a fixed singleton key for the sweep-format-drift item, `flight_plan:<stem>:<hash of the Blocker value>`, `marker:<rel-path>:<hash of the flagged text>` | — | DRAFT | source-read | EXACT |
| `di-sidecar-write-read-split` | The age sidecar is written only on a real regeneration, never under `--check` | Run `decision-inbox.py --check` against a root with a stale sidecar, then diff the sidecar file before/after | `receipts/desk/inbox-first-seen.json` is unchanged by `--check` (it computes the same ages for comparison but never persists); only `do_generate` (a real regeneration) writes it, append-only — a key that stops appearing moves into a `retired` sub-map with its original `first_seen` preserved, never deleted, and revives with that original date if the item reappears | — | DRAFT | source-read | EXACT |
| `di-backlog-item-text` | Backlog item text (pre-age-tail) cites the entry id, title, and its proposed fix | For a `### v<X>-<N> — <title>` backlog heading whose body matches `operator-gated` (either casing) and does not contain `RESOLVED` anywhere in the entry | Item `<text>` equals `Backlog v<X>-<N>: <title>` followed, when the entry body carries a `Proposed fix:` field, by ` — proposed: <its first sentence>` (A2, plain-language sweep Q4: the operator sees what is being ASKED without a round-trip into the backlog file); details tail is exactly `harness-backlog.md` | — | DRAFT | source-read | EXACT |
| `di-sweep-item-text` | Sweep item text (pre-age-tail) is the FULL item prose plus the briefing's file date | For a numbered item (its numbered line plus any indented continuation lines, joined; a trailing `(details: ...)` tail stripped) under SWEEP-BRIEFING.md's "Needs your attention" heading | Item `<text>` equals exactly `Sweep finding: <full-prose> (sweep dated <file-mtime-ISO-date>)` (A2, plain-language sweep Q1: the former first-sentence capture systematically deleted the consequence and fix sentences — exactly what a decision needs); an item whose prose carries the contract's consented-self-fix phrasing (`... with a yes`) renders under `Fixes the system can do on your yes (from the last sweep)` instead of the sweep decision section; an item whose prose is about this inbox itself is skipped with a stdout NOTE (self-reference guard, Q9) | — | DRAFT | source-read | EXACT |
| `di-flight-plan-item-text` | Flight-plan item text (pre-age-tail) carries the plan's file stem and blocker value | For a `**Blocker:** <value>` line (optionally bulleted, e.g. `- **Blocker:** ...`) in `wiki/flight-plans/<stem>.md` whose value is non-empty and not exactly `None` (trailing period ignored, case-insensitive) | Item `<text>` equals exactly `Flight-plan blocker (<stem>): <value>`; details tail is exactly `wiki/flight-plans/<stem>.md` | — | DRAFT | source-read | EXACT |
| `di-marker-item-text` | Marker item text (pre-age-tail) is the full marker line, self-reference excluded; identity stays clip-hashed | For a `DECISION-PENDING:` marker on any line of a root-level `*.md` file or a one-level `wiki/*.md` file (not `wiki/flight-plans/`) | Item `<text>` equals exactly `Flagged: <the full rest of the line after the marker token, stripped>` (A3 — was a 200-char clip, which deleted the consequence-and-fix sentences the reporting contract requires; first contractual multi-sentence producer: the compile skill's Step 3c verify-signal items); the item's sidecar identity hashes the first 200 characters only, so the A3 widening reset no existing item's first-seen age and a reword past char 200 keeps the age honest (A2's Q8 rule); `DECISIONS-PENDING.md` and `SWEEP-BRIEFING.md` are never scanned for this marker even when present at the root (both are projections/reports, never sources — self-reference-loop guard) | — | DRAFT | source-read | EXACT |

## Amendments

- **A1** | date 2026-07-24 | row: source_artifacts | prior: deploy/decision-inbox.py @ sha256 e4cd0313299b178998caae24d5b0c2558abe8d21c0aa04f748c7f1360f78ec22 | new: deploy/decision-inbox.py @ sha256 a4114073145af31fb5b4c994dcb5a03592fb721001097cda21af5fb94f04611d | provenance: git:a504d2e — sidecar-docstring clarification (revival = the single append-only exception, no first_seen ever lost); no row behavior changed, source-pin refresh only

- **A2** | date 2026-08-05 | rows: 1 new (`di-action-channel-line`), 5 amended (`di-item-line-shape` — checkboxes removed; `di-sweep-item-text` — full-prose capture + consented-fix routing + self-reference guard; `di-backlog-item-text` — proposed-fix carried; `di-age-tail-and-identity` — normalized-prose hash + backlog-id inheritance + cross-source dedup; `di-section-order` — the consented-fix section, last) | prior: 12 rows | new: 13 rows | provenance: the 2026-08-05 plain-language sweep (`audits/2026-08-05-plain-language-sweep.md`, findings Q1/Q2/Q4/Q8/Q9/Q11/Q12: the inbox truncated away exactly the consequence-and-fix sentences a decision needs, rendered dead checkboxes that regeneration silently erased, let chores masquerade as decisions, and reset its own age counters on wording churn). Producer self-test extended to cover every change (cases 26–29; **119/119**). Source pin re-hashed — which also heals the STALE pin check-manifest had been failing on this manifest since the producer's v3.0.22-era edits (the extraction note below predicted exactly this). FYI section retitled to plain language ("background wrap-ups retrying themselves"); the all-clear line and header comment are unchanged. Status stays EXTRACTED; rows stay DRAFT (no validator owed at this stage).

- **A3** | date 2026-08-09 | rows: 1 amended (`di-marker-item-text` — full-line capture, identity clip-stable) | prior: 13 rows | new: 13 rows | provenance: the verifier-demotion build (operator-approved design 2026-08-09, `harness-v3.0/specs/verifier-demotion-design-2026-08-09.md` §5). The compile skill's new Step 3c routes recorded-class verify signals through this scanner as its inbox pipeline, and its contractual wording is multi-sentence by design (verbatim checker quote + consequence-of-inaction + the two answers, per the reporting contract). The old 200-char clip deleted exactly those sentences — the same defect A2 killed on the sweep source (Q1), reproduced by the producer self-test before the fix. Display widens to the full marker line; the sidecar identity DELIBERATELY keeps hashing the first 200 characters, so no pre-A3 item's age reset (every pre-A3 item was ≤200 chars: clip == full) and rewording past char 200 keeps the age honest (A2's Q8 rule). Producer self-test extended (cases 8e; **123/123**). Source pin re-hashed. Status stays EXTRACTED; rows stay DRAFT.
