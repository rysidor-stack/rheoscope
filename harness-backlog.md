# Harness Backlog

*Canonical log of issues, gaps, and improvement candidates that are properties of the **harness template** (not this project's content), surfaced during real use. Each entry is a candidate contribution back to the harness upstream. Append entries as you hit them — the `/log-backlog` skill formats and numbers them for you. (In the template repo itself the skill is not installed; apply `core/skills/log-backlog/SKILL.md.template` manually — see `CONTEXT.md` § /preflight-on-self.)*

*What belongs here vs. project content: see `core/governance/CLAUDE.md` § Session discipline → Backlog logging.*

## Entry format

```markdown
### v<version>-N — Short title
- **Surfaced:** date and context (which phase / step)
- **Where:** file paths in the harness affected
- **Severity:** blocking / standard / nice-to-have
- **Symptom:** what the operator hits
- **Patched (project-local):** workaround applied, if any (omit the field if none)
- **Proposed fix:** how to address upstream
- **References:** commits / docs / session logs that discuss this
- **Status:** OPEN
```

## Closure convention (one format, 2026-08-08)

Every entry's LAST bullet is exactly one status line, machine-parseable:

```
- **Status:** OPEN
- **Status:** CLOSED <YYYY-MM-DD> — <one-line disposition>
```

Grep contract: `^- \*\*Status:\*\* (OPEN|CLOSED \d{4}-\d{2}-\d{2} — .+)$` matches every
entry's status line (plus the two documentation examples in this section and the entry-format
block — subtract 2, or scope the grep below the last `---`). Closing an entry means editing its status line (the
one sanctioned edit to an existing entry) — the older free-form `**CLOSED/LANDED/RESOLVED/
BUILT ...**` narrative bullets stay as the closure's detail record, but the status line is
the single machine-readable verdict. The release-time triage promised below is now an
executable step: sweep `Status: OPEN` lines, adjudicate each, and empty what lands. A
status line is normalization, not adjudication: an entry whose work may have shipped but
whose entry text never recorded it stays OPEN until a triage session closes it with a
disposition.

## Versioning and numbering

- **The version prefix is read at runtime, never hardcoded.** In an instantiated project, `<version>` is `template_version` from `project.yaml` — the harness version the project actually runs (e.g. `v2.0-3` on a v2.0 harness). It changes only when the project migrates to a newer harness. In the harness template repo itself, `<version>` is the root `VERSION` file — the in-development release that will absorb the entries.
- **Numbering is sequential per version prefix** (`v<version>-N`, unpadded, restarting at 1 for each version). The prefix records which harness version an issue was found on; entries become candidate fixes for the harness's next release.
- **The file name is deliberately version-free** so it can never go stale; the version lives only in entry numbers. (Renamed from `v1.1-backlog.md` in v2.0 — see `adr/` in the harness template repo.)

## Severity meanings

- **blocking** — operator cannot proceed without a workaround
- **standard** — operator can proceed but has to invent a workaround or accept noise
- **nice-to-have** — quality-of-life or polish; no real blockage

## Process notes

- **Logging discipline.** Log a harness-template issue in the session it surfaces, via `/log-backlog`. Don't let issues scatter across commit messages and adaptation notes.
- **Stable numbering.** Once a `v<version>-N` number is assigned, it's permanent. Supersede with a new entry carrying `Supersedes: v<version>-N` rather than renumbering or rewriting — including across versions (a v2.1 entry may supersede a v2.0 one).
- **Append-only.** `/log-backlog` never rewrites existing entries; one entry per invocation.
- **Contribution back.** When the harness opens its next development cycle, this file is reviewed entry-by-entry — items either land, get reclassified, or get merged with related items.
- **Cross-project signal.** If a future project hits the same issue independently, note it in the entry's References. Repeated discovery is strong prioritization signal.
- **In the template repo:** this file doubles as harness-dev's own live backlog. At each release it is triaged entry-by-entry (land / defer to changelog / supersede) and emptied, so it ships empty to instantiated projects.

---

*(v2.0 triage, 2026-06-10, at ship: v2.0-1 closed — documented exclusion, recorded in the sensor docstring and the changelog #10 entry; v2.0-2 closed — fixed by the release-hardening commit, G2-verified; v2.0-3 closed — all four amendments landed via #9 and #7; v2.0-4/5/6/7 carried forward as v2.1-1…4 with `Supersedes:` links. Full entry texts remain in git history at the pre-ship commit.)*

*(v2.1 triage, 2026-06-12, at ship — tag `v2.1`: **closed** — v2.1-2 (capability-doc authoring rule, landed in `capabilities/INDEX.md` via #2), v2.1-3 (#4 execution-engine doc-polish trio, landed via #1), v2.1-4 (hardening-G2 nits + driver version pins, resolved via #2 and #14), v2.1-5 (minimal gate, landed as the operate-sentinel phase-1 recipe via #2). **Carried forward** to the next cycle (to be re-filed with `Supersedes:` links at cycle open): v2.1-1 (CLAUDE.md firewall-heading rename), v2.1-6 (residual universal PAN/CVV assertion in verification-architecture — agnosticism residual, standard), v2.1-7 (trio precondition back-link reciprocity), v2.1-8 (instantiating text cites harness-dev ADRs/item numbers — standard; the trio "backlog v2.1-5" pointers were resolved via #2, the ADR/item-number grounding remains), v2.1-9 + v2.1-10 (pilot-kit dev-tooling pointers, actionable only if the pilot-kit pattern is reused). Full entry texts remain in git history at the pre-ship commit.)*

---

### v3.0-1 — CLAUDE.md section heading "Builder/Verifier substrate firewall" names the wrong actors
- **CLOSED 2026-06-13** (this commit): renamed the heading `Builder/Verifier substrate firewall` → `Decision-lock substrate firewall` (`core/governance/CLAUDE.md.template` § Session discipline). The new name matches the body — which governs T1 *decision* locking via distinct authoring/locking substrates, not the code-firewall actors — and aligns the forward-facing heading with the established ADR vocabulary (`adr/DECISIONS.md` row 3 / ADR #3 already read "decision-lock substrate-firewall"; CONTEXT.md's glossary flags the code-vs-decision "firewall" collision the old heading triggered). Cite-sweep: the old string survives only in 5 frozen records (append-only backlog entry, tagged v1.1 changelog line, `verifier-reviews/v2.0-item-6-*`, `harness-v2.0/remaining-increments-analysis.md`) — deliberately preserved per the Rheoscope rename doctrine; no forward-facing citation, no broken/colliding anchor (the file cross-references via backtick code-spans, not `#anchors`). The generic "Builder/Verifier *firewall*" *concept* references (PROJECT-COMPASS, tier-definitions) are a different phrase (the code firewall) and correctly unchanged. Verified: G1 7/7 PS + 8/8 Unix first-hand at VERSION=3.0 (no-op instantiation confirmation — prose-only template edit); 5-lens adversarial review (cite-sweep / frozen-safety / naming / trio-doctrine / completeness) all PASS, high confidence (same-family Opus firewall; a true different-family pass is an optional non-blocking follow-up, per the v3.0-4 precedent).
- **Supersedes:** v2.1-1 (← v2.0-4); carried at the v2.1 ship 2026-06-12, full prior text in git history at the pre-ship commit
- **Surfaced:** 2026-06-10, #6 build + verifier review
- **Where:** core/governance/CLAUDE.md.template (§ Session discipline)
- **Severity:** nice-to-have
- **Symptom:** the heading names the CODE-firewall actors but the rule governs DECISION locking; #6's appended clause mitigates in the body.
- **Proposed fix:** rename pass (e.g. "Decision-lock substrate firewall") bundled with the next increment touching CLAUDE.md.template; update any spec text citing the heading in the same diff.
- **References:** verifier-reviews/v2.0-item-6-review.md; CONTEXT.md § Flagged ambiguities.
- **Status:** CLOSED 2026-06-13 — closure recorded in the entry's own CLOSED note above.

### v3.0-2 — Residual PAN/CVV assertion in core verification-architecture (ADR #4 Phase B miss)
- **CLOSED 2026-06-13** (this commit): generalized the residual `core/methodology/verification-architecture.md.template` § Evidence-Artifacts data-safety clause `…and PAN/CVV never appear in any artifact` → `…and no raw sensitive personal data appears in any artifact` — completing the ADR #4 (f) Phase B payment-cross-ref neutralization this file was missed by. "raw" matches `DATA-POLICY.md.template`:85's gate language ("no raw sensitive personal data ever") and `execution-engine.md.template`'s differential gate, so all three core data-safety surfaces now share one general frame; it also preserves DATA-POLICY's enumerated/ID-referenced-PII allowance (which is not "raw"). PAN/CVV stays in core only where ADR #4 (c) wants it — the labeled opt-in HARDCONSTRAINT example at `DATA-POLICY.md.template`:22 (a payment project re-adds it at INIT). **Lane-#5 caution resolved (RTDT):** the entry + `backport-scope.md` row 5 said "diff aces-wiki's neutral framing first (may move both ways)," but ADR #4 + backport-scope's forward-port flag settle the direction — the template is the agnostic/ahead side; aces-wiki legitimately keeps its payment domain rule as a real Stripe project. The template fix is fully determined by in-repo doctrine; the cross-instance forward-port *into* aces-wiki stays under v3.0-7 lane #5 / recommended-sequence step 3 (operator-private instance, not doable from this repo). **Verified:** exhaustive multi-domain residual sweep confirms this was the ONLY genuine payment-as-universal residual in the shipped surface (no siblings; all other domain refs are labeled examples / opt-in); G1 7/7 PS + 8/8 Unix first-hand at VERSION=3.0; 5-lens adversarial review (adr4-fidelity / sibling-consistency / residual-sweep / scope-safety / completeness) all PASS, high confidence. **Firewall note (T1-adjacent):** same-family Opus in-session firewall; a true different-family review is the recommended but non-blocking follow-up (Claude-only in-session, not dispatchable), per the v3.0-4 precedent.
- **Supersedes:** v2.1-6; carried at the v2.1 ship 2026-06-12
- **Surfaced:** 2026-06-11, #14 verifier review (finding N2, report-only)
- **Where:** core/methodology/verification-architecture.md.template (§ Evidence Artifacts and Data Safety)
- **Severity:** standard (an agnosticism residual ships into every instantiated project; not operator-blocking)
- **Symptom:** a core data-safety paragraph bakes in the universal assertion "…and PAN/CVV never appear in any artifact" — a payment-domain specific stated as if universal, not framed as opt-in. Contradicts ADR #4 (c)/(f).
- **Proposed fix:** generalize to "sensitive personal data," or convert to an opt-in reference. Rides the next increment touching verification-architecture.md.template; may sweep with v3.0-4. (Back-port lane #5 may move this both ways — diff aces-wiki's neutral framing first.)
- **References:** verifier-reviews/v2.1-item-14-review.md (N2); adr/2026-06-11-4-agnosticism-remediation.md; harness-v3.0/backport-scope.md row 5.
- **Status:** CLOSED 2026-06-13 — closure recorded in the entry's own CLOSED note above.

### v3.0-3 — Precondition-trio back-links not reciprocal in the two earlier docs
- **CLOSED 2026-06-13** (this commit): made the precondition-trio closing back-links reciprocal/symmetric. `least-privilege-isolation.md.template`'s `**Sibling precondition:**` line (named only rollback-kill-switch) now names both siblings (+ spend-governance) and reads "all three"; a new symmetric `**Sibling preconditions:**` line was added to `rollback-kill-switch.md.template`'s Forward Hooks (it had no closing cross-link); `spend-governance.md.template`'s "The precondition trio." line was already complete. **Backlog-framing correction (RTDT):** the symptom said "the two earlier docs name only their pairwise sibling," but post-v3.0-4 both docs' grounding bullets already name all three preconditions by concept — the genuine remaining gap was only the *closing file-path cross-link* (one line edited in least-privilege; one line **added** in rollback-kill-switch, which is an addition, not the "extend an existing line" the entry assumed). Prose symmetry only — the load-bearing reciprocity already holds via the shared `AUTOMATION-ISOLATION.md` + methodology `INDEX.md` anchors (confirmed unchanged); role descriptors scope/stop/cap verified factually correct, no semantic drift. Verified with the same G1 run (7/7 PS + 8/8 Unix at VERSION=3.0) + the 5-lens adversarial review (trio-doctrine + completeness lenses PASS, high confidence).
- **Supersedes:** v2.1-7; carried at the v2.1 ship 2026-06-12
- **Surfaced:** 2026-06-11, #12 verifier review (Nit 2, report-only)
- **Where:** core/methodology/least-privilege-isolation.md.template + rollback-kill-switch.md.template (sibling-precondition lines)
- **Severity:** nice-to-have
- **Symptom:** spend-governance.md names all three precondition siblings; the two earlier docs name only their pairwise sibling and don't point forward to spend-governance.md. The load-bearing reciprocity already holds via the shared AUTOMATION-ISOLATION.md + methodology INDEX anchors; prose symmetry only.
- **Proposed fix:** add the third back-link to the sibling-precondition reference in each of the two earlier docs (one line each). Rides the next increment touching either file, or a tiny dedicated pass.
- **References:** verifier-reviews/v2.1-item-12-review.md (Nit 2).
- **Status:** CLOSED 2026-06-13 — closure recorded in the entry's own CLOSED note above.

### v3.0-4 — Instantiating text cites harness-dev ADRs / item numbers / backlog entries (trio docs + methodology INDEX)
- **CLOSED 2026-06-13** (this commit): translated all harness-dev references in the four shipped files (`core/methodology/{rollback-kill-switch,least-privilege-isolation,spend-governance}.md.template` + `INDEX.md`) into self-contained, project-meaningful statements — ADR-#1 commitment/trigger citations, the "Grounding in ADR #1 (scope lock)" headings, and item-number shorthand (#1/#2/#3/#11/#12/#14) all gone; doctrine substance, the precondition-trio / stop-scope-cap framing, and the recipe pointers unchanged; historical records (tagged changelog, ADRs, `handoffs/`, `harness-v2.0/`, `verifier-reviews/`) deliberately retain their harness-dev refs. Verified: grep clean (`ADR #` / `commitment (` / `reopen trigger` / `#11` / `#12` / `#14` → none; no bare `#1/#2/#3` capability shorthand); G1 7/7 PS + 8/8 Unix first-hand (drivers verbatim); fresh-context cross-model fidelity read APPROVE on meaning-drift/residue/coherence/scope (a true different-family review wasn't dispatchable in-session — optional non-blocking follow-up). Shape per operator 2026-06-12: **TRANSLATE** (not strip, not keep-as-provenance).
- **Supersedes:** v2.1-8 (partial — the trio "backlog v2.1-5" pointers were resolved via v2.1 #2; the ADR/item-number grounding remains); carried at the v2.1 ship 2026-06-12
- **Surfaced:** 2026-06-12, v2.1 #1 build (the D3 grep-driven residue sweep)
- **Where:** core/methodology/{rollback-kill-switch,least-privilege-isolation,spend-governance}.md.template (the "Grounding in ADR #1" sections + #1/#2/#3 forward hooks); core/methodology/INDEX.md (ADR #1 ties in three loading-order rows)
- **Severity:** standard (ships into every instantiated project; not operator-blocking)
- **Symptom:** instantiated projects receive doctrine whose grounding cites ADR #1 commitments and harness item numbers (#1/#2/#3/#11/#12/#14) — none of which exist in a project. Honest provenance in the template repo; dangling references for the project reader.
- **Proposed fix:** one dedicated pass translating harness-dev references into project-meaningful language (a self-contained statement of the commitment instead of "ADR #1 commitment (d)"). Operator call on shape: strip vs. translate vs. keep-as-provenance-note. This is the early-next-cycle agnosticism pass named at the v2.1 ship triage — its own reviewed pass, not doc-polish riding an unrelated diff. **Shape decided (operator, 2026-06-12): translate** to project-meaningful self-contained statements (e.g. "the parallel-build capability" / "the enablement preconditions" instead of "#1" / "ADR #1 commitment (d)") — not strip, not keep-as-provenance. **Surface is larger than a tidy** (~20+ refs used as structural shorthand across the trio + INDEX); a different-family verifier review is recommended (T1-adjacent doctrine, shipped through three APPROVE reviews). NB: the forward-facing Rheoscope rename (2026-06-12) already renamed the trio docs' provenance-header slug; v3.0-4 is the remaining grounding/forward-hook translation.
- **References:** verifier-reviews/v2.1-item-14-review.md, v2.1-item-11-review.md, v2.1-item-12-review.md; HARNESS-CHANGELOG.md (v2.1 ship triage); harness-v3.0/backport-scope.md rows 4/5.
- **Status:** CLOSED 2026-06-13 — closure recorded in the entry's own CLOSED note above.

### v3.0-5 — Pilot-kit I9 seal: `git status --porcelain` blind spot when the kit is untracked
- **Supersedes:** v2.1-9; carried at the v2.1 ship 2026-06-12
- **Surfaced:** 2026-06-12, v2.1 #1 verifier review (Nit 1, non-gating)
- **Where:** verifier-reviews/v2.1-item-1-pilot/ (the `seal_self_assert` path in `run-pilot.ps1`) — **pilot-kit dev tooling, NOT harness-template surface**
- **Severity:** nice-to-have
- **Scope note:** project-local pilot-kit tooling, not a shipped-template defect. Actionable only **if the pilot-kit pattern is ever promoted toward reuse**, else close.
- **Symptom:** the I9 seal half comparing `git status --porcelain` before/after a run can't detect writes into a subdirectory of an already-untracked kit folder (porcelain collapses the untracked tree to one line). The seal held in #1 (the worktree-list half is airtight; the driver writes only to %TEMP%).
- **Proposed fix:** run the seal from a clean tree and/or add a recursive inventory diff alongside the porcelain + worktree-list comparison — if/when the pattern is reused.
- **References:** verifier-reviews/v2.1-item-1-review.md (Nit 1 + disposition header).
- **Status:** OPEN

### v3.0-6 — Pilot-kit `kill-switch.ps1` halts by stale raw PID from an unpruned registry
- **Supersedes:** v2.1-10; carried at the v2.1 ship 2026-06-12
- **Surfaced:** 2026-06-12, v2.1 #1 verifier review (Nit 2, non-gating)
- **Where:** verifier-reviews/v2.1-item-1-pilot/kill-switch.ps1 (+ its `pids.json` registry) — **pilot-kit dev tooling, NOT harness-template surface**
- **Severity:** nice-to-have
- **Scope note:** as v3.0-5 — pilot-kit tooling, not a shipped defect. The shipped doctrine (KILL-SWITCH.md / rollback-kill-switch.md.template) contains no such script. Actionable only if the pattern is promoted.
- **Symptom:** the pilot halt reads `pids.json` (an unpruned whole-run registry) and kills any still-alive PID by raw PID; a recycled OS PID could in principle be killed. Negligible in the ~30s pilot run.
- **Proposed fix:** scope the halt to a run-scoped job object / process group and prune on completion — if/when the pattern is reused.
- **References:** verifier-reviews/v2.1-item-1-review.md (Nit 2 + disposition header).
- **Status:** OPEN

### v3.0-7 — Back-port triage (aces-wiki ⇄ template drift) — master list seeds per-lane entries
- **Surfaced:** 2026-06-12, v3.0 cycle open
- **Where:** harness-v3.0/backport-scope.md (the master drift map — 11 lanes, both directions)
- **Severity:** standard (methodology drift between the v2.x reference instance and the template; not operator-blocking)
- **Symptom:** the live aces-wiki instance and the template have drifted both ways. The lanes are mixed: genuine back-ports (instance sharpened under load), one structural design call (lane #2 dispatch-envelope vs execution-engine — **a T2 handoff, not a backlog nit**), several NEEDS-DIFF (direction unproven until a file-level diff), one DON'T-PORT (lane #11, superseded by the v3.0 memory engine), and two forward-ports (template → aces-wiki, tracked instance-side, not here).
- **Proposed fix:** per `backport-scope.md` § Recommended sequence — (1) settle the NEEDS-DIFF pairs (#4/#5/#6/#8/#9/#1) with real diffs, splitting each into its own v3.0-N entry as scheduled; (2) make the #2 structural call via a substrate-different T2 handoff (#3 rides it); (3) forward-port the safety regime into aces-wiki before the v3.0 daemon/engine depends on it; (4) port the neutral patterns (#7 corpus observation; #6 co-residency if the diff confirms); (5) knowledge-os lane (#8/#9) as its own pass. Lane #5 overlaps v3.0-2; lane #4 overlaps v3.0-4. (Registered as one umbrella entry rather than 10 premature NEEDS-DIFF entries — split as each lane is scheduled.)
- **References:** harness-v3.0/backport-scope.md; harness-v3.0/README.md § Relationship to the v2.1 cycle.
- **Update 2026-06-13:** lane #5 (verification-architecture) **template-half discharged** by v3.0-2 (PAN/CVV → "no raw sensitive personal data" generalization, committed this cycle). Lane #4 (execution-engine) template-half similarly already discharged by v3.0-4. Remaining NEEDS-DIFF lanes to split per-lane: #1, #6, #8, #9 (+ the #2 structural T2 handoff).
- **Plan note 2026-06-13 (operator — reframes this entry):** the **back-port direction (aces → template) is the load-bearing part** of v3.0-7. The v3.0 endgame is: finish v3.* in the template → **fork** the finished template → pour aces's project content (knowledge database + wiki + raw) into the fork → **dogfood** the v3.0 template against that running project. Live `aces-wiki` and the finished template both stay **untouched**; the fork is the experiment. Consequence: aces's methodological learnings the template lacks must be folded **into the template before the fork**, or the fork loses them — but the lanes split two ways. **Clean back-ports (fold into the template):** co-residency #6, corpus-observation #7, knowledge-os rules #8/#9. **Engine-territory (NOT mechanical back-ports):** handoff protocol #1 is **most likely usurped by the v3.0 handoff-engine upgrade** (`harness-v3.0/specs/handoff-engine-spec.md` — portfolio rounds / premise ledger / calibration / daemon), so aces's handoff sharpening (3→1 consolidation, round caps, convergence check, packet inline/reference + pinned-manifest, supersession fast path, Mode A/B) becomes **input to that redesign**, not a standalone template edit; the dispatch-envelope #2 call is likewise engine-territory. Both #1 and #2 sit on ADR #1 (f) deferred-#5 ground and route through a **fresh handoff** before any build.
- **Update 2026-06-14 (back-port lanes #6/#7 settled, under the ADR #5 close session):** **#6 taint co-residency invariant — BACK-PORTED.** The template's isolation regime (credential scope for autonomous capabilities) did NOT subsume the reference instance's *session-composition* invariant (untrusted content + credentials + egress never co-resident in one session), so it was ported agnostically into `core/governance/CLAUDE.md.template` § Security perimeter (+ a reciprocal pointer in `least-privilege-isolation.md.template`), stripping instance specifics (website-recon / Bizzflo / git-push-vps / raw/) per ADR #4. Lands the contract-layer invariant `fork-dogfood-plan.md` Correction 1 + memory-engine spec §13 both place in CLAUDE.md. G1 GREEN 7/7+8/8; same-family adversarial verifier pass clean. **#7 corpus-observation — ALREADY COVERED (no port).** The principle is already in the template — fuller, agnostic, `corpus_source`-gated — at `capabilities/knowledge-os/extracted/wiki-schema.md.template § 16` + the compile/audit/flight-plan skills (the template is a *superset* of the source). The drift-table's "template home: none / BACK-PORT the principle" is **stale** (predates the knowledge-os extraction). **Remaining clean-back-port lanes:** #8/#9 (knowledge-os format contracts + orchestrator skills) — operator provided the reference instance's rule/skill files; the diff pass is next.
- **Update 2026-06-15 (back-port lanes #8/#9 settled — closes the clean-back-port set):** **#8 knowledge-os format contracts — BACK-PORTED (8 deltas).** Diffed the reference instance's six `.claude/rules/` files against `capabilities/knowledge-os/extracted/wiki-schema.md.template` (one lane, not file-by-file) and ported the genuine generalizable deltas the template lacked, stripped per ADR #4: §2 decision-lock raw files (`informed_by:` raw↔handoff back-link, gated on the handoff protocol); §3 machine-parsed lead-in (precision is load-bearing; don't drop frontmatter keys); §5 machine-walked clause + pointer to the `/audit` skill for the full grading protocols; §7 receipt-honesty discipline + authorized-removal record (`operation: removed`, enum updated) + shared-envelope note + a trimmed bounded-changelog note; §9 HEALTH coverage-row ↔ Directory-Preservation invariant; §10 "sources win" projection invariant + section-level carry-forward discipline; §13 correction-line format. **#9 orchestrator skills — compile BACK-PORTED (8 deltas), audit ALREADY-COVERED.** `compile/SKILL.md.template`: Step 1 `pending_cascade` re-entry + block-robust frontmatter parse guard; Step 3 fan-out-reads + completion rule; Step 5.5 first-observation corpus seeding; Step 6 current-state-projection + carry-forward summaries; **new Step 7.5 SESSION-BRIEFING regeneration** (closes a real spec-vs-skill gap — wiki-schema §10 promised `/compile` regenerates it, but the skill never did); Step 11 YAML-parseable receipt rule; circuit-breaker "substantive"-article refinement. `audit/SKILL.md.template` was found **already-covered** — a faithful agnostic generalization of the instance (grading protocols / assumption types / REVIEW format already match), so no deltas ported; the only audit edit was an agnosticism scrub of a pre-existing residual (`sqitch.plan` → "the deployed change manifest", ADR #4 re-open trigger 1) caught in the sweep. **Skipped/flagged (verified not genuine missing deltas):** multi-source corpus iteration (needs `corpus_source`-as-a-list — a `project.yaml` schema change; routed to v3.0-8 packaging); the flight-plans Layer-1-projection discipline + FLIGHTDECK / dispatch-envelope (core/flight-plan + lane-#2 engine territory, out of knowledge-os scope — **flagged for the operator**); the determinism-layer scripts (`compile-preflight.py` et al., instance tooling); the Decision-Tracker single-homing rule (flight-plan-specific). **Verified:** G1 GREEN 7/7 PS + 8/8 Unix first-hand (`capabilities/**/.template` touched ⇒ G1 applies); agnosticism grep clean over the edited surface; a same-family (Opus) **4-lens adversarial firewall** (agnosticism / fidelity / RTDT-over-port / coherence-completeness, each re-deriving from source — anti-framing-capture) found the core clean (no leaks, `{{...}}` vars preserved, audit-superset + the Step-7.5 gap-closer independently confirmed). Findings adjudicated: trimmed an over-ported bounded-changelog to a one-line hygiene note (3 lenses converged; the template's changelog sits in no hot machine-read path and ships no rotation tooling), added `removed` to the receipt `operation` enum (coherence), and fixed two self-caught coherence items (§14 SESSION-BRIEFING writer credit; Step 7.5 instance section-name). Same-family ⇒ rung-1 only; a true cross-vendor / different-family review remains the OPTIONAL operator-fired follow-up (v3.0-2/-4 precedent; not Claude-dispatchable in-session). **This closes the clean-back-port lanes (#6/#7/#8/#9) the fork-dogfood plan's forkable-baseline (item 2) needed.** Remaining forkable-baseline gate: the **MEMORY CONTRACT LAYER** (derivation-block schema + `check-frontmatter` schema-checker, opt-in) — NOT started this session.
- **Status:** OPEN

### v3.0-8 — Memory-engine spec/test-plan carries aces-specific paths + unconditional corpus gates (agnosticism re-contamination risk)
- **Surfaced:** 2026-06-13, v3.0 endgame 6-lens adversarial assessment (template-integrity lens, rated blocking)
- **Where:** `harness-v3.0/specs/memory-engine-v3-test-plan.md` (LLM-6 CORPUS-SUPPORT, ~line 412 — hardcodes `C:\Users\Jido\…\aces-platform` / `aces-stack` into a permanent every-compile gate); `harness-v3.0/specs/memory-engine-v3-spec.md` §13 (template-layer deliverables described as shipping unconditionally).
- **Severity:** standard (T1-adjacent agnosticism; would ship aces-specifics into every instantiated project — undoes ADR #4 — and breaks non-knowledge-os instantiation)
- **Symptom:** the memory engine's verification machinery hardcodes the aces operator's machine paths in a binding gate, and ships corpus-support as a mandatory P3 gate even for greenfield projects with no wiki/corpus. Re-contamination + cryptic failures for non-knowledge-os projects.
- **Proposed fix:** before any template ship of the memory machinery — (a) parameterize/fixture the corpus-repo paths (config, never hardcode); (b) gate corpus-support (LLM-6) behind knowledge-os activation / corpus-present (skip clause when `wiki_domains`/corpus unconfigured); (c) package the memory engine as the **opt-in knowledge-os capability's v3 form**, not a universal GA layer. NB: the spec + its verification record are immutable/certified-as-scoped — this is capability *packaging* + a fixture/config concern, **not** a spec edit (sibling-brief if the framing needs to change).
- **References:** `harness-v3.0/fork-dogfood-plan.md` (Correction 2); the 6-lens assessment; `adr/2026-06-11-4-agnosticism-remediation.md`; `engine-verification-ledger.md` (LLM-6).
- **Routed 2026-06-14:** the gating + path-parameterization decision is now carried in the fresh handoff `handoffs/2026-06-14-v3-engine-licensing-and-firewall-merge/` (brief Q6; packet § 5E/§ 5F). Still **doable-now without the handoff** as a concrete template fix (the handoff resolves the framing, not the code). Remains a **blocking prerequisite before any template ship** of the memory machinery.
- **Addressed 2026-06-14 — sibling brief authored:** the doable-now portion is discharged as a **binding packaging + path-parameterization brief**, [`harness-v3.0/specs/memory-engine-v3-packaging-brief.md`](harness-v3.0/specs/memory-engine-v3-packaging-brief.md) (per the harness-v3.0 README Rules of the road: the spec/test-plan are **immutable**, so the fix is a sibling brief, **not** a spec edit). **Finding:** the shipped surface (`core/`, `project.yaml.example`, `capabilities/knowledge-os/`) is **already clean** — the config surface already exists (`capabilities.knowledge-os` toggle + `corpus_source`/`corpus_config` runtime-read + `wiki_domains`); the contamination is confined to the two **immutable** design specs (`test-plan:412`, `spec:129`), read as instance-illustrative. The brief binds **R1** (paths from config, never hardcode) + **R2** (corpus gates skip when no corpus / knowledge-os off) + **R3** (opt-in knowledge-os v3 packaging) onto the fork build, with **A1–A3** as the ship-gate. **Residual:** build-time compliance (verified at ship via A1–A3) — the entry stays a **ship-gate** (not fully closed), because nothing is built yet.
- **Status:** OPEN

### v3.0-9 — Reference-instance cutover path (aces-wiki) undefined
- **Surfaced:** 2026-06-13, v3.0 endgame 6-lens assessment (completeness lens, rated blocking)
- **Where:** `harness-v3.0/specs/memory-engine-v3-spec.md`:10–12 ("the instance conforms only after the plan is proven and the cutover is gated and reversible" — conditional language, not a commitment); no cutover runbook anywhere.
- **Severity:** standard (process/doctrine gap; the live business system's fate is an unscheduled decision)
- **Symptom:** the plan keeps live aces-wiki untouched and proves the engine on the fork, but never defines if/when/how aces-wiki receives the proven engine — no phase model, no reversibility window, no success criteria, no cutover rollback runbook. The MIG-1/GOLD-1 gates prove the fork, not the cutover.
- **Proposed fix:** record an explicit decision (ADR-style) — aces-wiki cutover is either **in-scope** for v3.0 (then add it to the fresh-handoff license + define the gated/reversible sequence) or **explicitly deferred** post-dogfood (then capture the success criteria for eligibility, e.g. "all 39 gates pass + N weeks of fork stability"). T1 (business system). Generalizes to a harness "reference-instance cutover" doctrine.
- **References:** `harness-v3.0/fork-dogfood-plan.md` § Open Decisions (#1); `engine-verification-ledger.md` (MIG-1/GOLD-1 prove the fork).
- **Routed 2026-06-14:** the in-scope-vs-deferred cutover decision is carried in the fresh handoff `handoffs/2026-06-14-v3-engine-licensing-and-firewall-merge/` (brief Q7a; deliverable shape requires a disposition). Resolved at the handoff's lock (→ ADR #5).
- **RESOLVED 2026-06-14 — LOCKED ([ADR #5](adr/2026-06-14-5-v3.0-engine-licensing-and-firewall-merge.md)):** **explicitly DEFERRED post-dogfood** with named eligibility criteria (e.g. all 39 build gates pass + a declared fork-stability window + a separate **reversible cutover ADR**). aces-wiki stays untouched until that decision; MIG-1/GOLD-1 prove the fork, not the cutover. Generalizes to a harness "reference-instance cutover" doctrine. **Entry closed.**
- **Status:** CLOSED 2026-06-14 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-10 — Fork-build rollback / abort plan missing
- **Surfaced:** 2026-06-13, v3.0 endgame 6-lens assessment (completeness lens, rated blocking)
- **Where:** no doctrine; the precondition trio (`rollback-kill-switch.md.template` et al.) covers the v2.x harness, not the on-fork engine build.
- **Severity:** standard
- **Symptom:** no defined fallback if the memory-engine fork build fails its gates (MIG-1 abort criteria; OPS-4 cost blowout >2× tokens / >2× latency vs old-path). No per-phase abort→recover steps, no spend-cap auto-kill, no operator decision record.
- **Proposed fix:** define a fork-build rollback plan — per-phase abort criteria (partly already in MIG-1/OPS-4), rollback steps (the fork is disposable: discard + retain lessons; live aces-wiki is never touched, so safety holds by construction), a spend-cap envelope with auto-kill, and the operator decision record. Add to the fresh-handoff scope. Generalizes to a harness "build-on-fork rollback" doctrine.
- **References:** `harness-v3.0/fork-dogfood-plan.md` § Open Decisions (#2); `core/methodology/rollback-kill-switch.md.template` (covers the harness, not the fork build); `core/methodology/spend-governance.md.template` (the cost envelope). The **forward-port direction is fork-inherited** — the fork is built from the finished template, so it gets the agnosticism + precondition-trio abstractions by construction; `backport-scope.md`'s recommended-sequence **step 3 ("forward-port the safety regime into aces-wiki") is superseded** by the fork mechanism. (NB: capturing the back-ports still needs aces's methodology files brought into a session to diff — the template repo can't see aces-wiki.)
- **Routed 2026-06-14:** the "build-on-fork rollback" doctrine decision is carried in the fresh handoff `handoffs/2026-06-14-v3-engine-licensing-and-firewall-merge/` (brief Q7b; deliverable shape requires a disposition). Resolved at the handoff's lock (→ ADR #5).
- **RESOLVED 2026-06-14 — LOCKED ([ADR #5](adr/2026-06-14-5-v3.0-engine-licensing-and-firewall-merge.md)):** **ADOPT a named build-on-fork rollback doctrine** — per-phase abort criteria (MIG-1/OPS-4 triggers), spend-cap auto-kill, operator decision record; default action = discard fork + retain lessons (live aces-wiki never touched, so safety holds by construction). The doctrine itself is **AUTHORED 2026-06-25** → [`harness-v3.0/build-on-fork-rollback-doctrine.md`](harness-v3.0/build-on-fork-rollback-doctrine.md); the decision to have one was locked at ADR #5. Generalizes to a harness "build-on-fork rollback" doctrine. **Entry closed.**
- **Status:** CLOSED 2026-06-14 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-11 — Dev `handoffs/` not placeholder-scan-exempt → init-validate fails when a handoff packet inlines/quotes `{{...}}`
- **CLOSED 2026-06-14** (this session): added `handoffs/` to `placeholder_scan_exempt` in `project.yaml.example` (the permutation driver derives each permutation's `project.yaml` from it — `verifier-reviews/v2.0-item-10-run-permutations.sh:25`), matching how the other dev-only dirs (`adr/`, `harness-v2.0/`, `verifier-reviews/`) are already exempted. The trailing-slash prefix matches root dev `handoffs/` but **not** `core/handoffs/` — a project's own runtime handoffs are authored to `core/handoffs/<slug>/` and stay scanned (verified equivalent in `init-validate.sh:51` and `init-validate.ps1:44`). **G1 re-run GREEN 7/7 PS + 8/8 Unix.**
- **Surfaced:** 2026-06-14, the first G1 run after the v3-engine-licensing handoff folder was committed (ADR #5 close, commit `78c3bf9`) — the first dev handoff whose packets reference/inline literal `{{...}}` (the 2026-06-08 handoff has none, so the gap was latent). init-validate flagged `handoffs/2026-06-14-.../{context,packet-round-1,packet-round-5}.md` in every permutation, leaving `main` briefly G1-red until this fix.
- **Where:** `project.yaml.example` (placeholder_scan_exempt); `init-validate.{sh,ps1}` (the scanner that reads it).
- **Severity:** standard (a committed dev handoff with `{{...}}` content turns G1 red; not operator-blocking once known).
- **Fix:** exempt `handoffs/` (dir prefix). Safe because (a) at init-validate time a real project's `handoffs/` is empty and project handoffs land under `core/handoffs/` (still scanned), and (b) it follows the established dev-dir exempt pattern (init.sh has no copy/exclude filter — confirmed).
- **Same-class latent gap — FIRED + FIXED 2026-06-15:** as predicted, `harness-v3.0/` was the next break — the new `memory-engine-v3-contract-layer-brief.md` (a strand brief that references literal `{{...}}` when scoping the template) tripped the placeholder scan in every permutation. Fixed by adding `harness-v3.0/` to `placeholder_scan_exempt` (the same dir-prefix pattern as `handoffs/`); G1 re-run GREEN 7/7 PS + 8/8 Unix. The "add it then" trigger is now discharged.
- **Verified:** same-family adversarial verifier pass (4-lens: agnosticism / placement / init-fix-safety / RTDT, each non-nit finding adversarially re-verified) over this fix + the #6 back-port — clean, no blocking/standard defects; a true cross-vendor review remains the optional operator-fired follow-up.
- **References:** commit `78c3bf9` (ADR #5 close); `verifier-reviews/v2.0-item-10-run-permutations.{sh,ps1}`; `init-validate.sh:39-59`.
- **Status:** CLOSED 2026-06-14 — closure recorded in the entry's own CLOSED note above.

### v3.0-12 — Secret-scanner pre-commit gate (contract-layer B-general) not built
- **Surfaced:** 2026-06-20, the memory-engine contract-layer build (B-general security perimeter).
- **Where:** `core/security/` ships only PreToolUse hooks (`block-dangerous-bash.sh`, `block-env-writes.sh`); there is no git pre-commit secret-scanner.
- **Severity:** standard (defense-in-depth hardening; not blocking the contract layer or the fork build).
- **Symptom:** the contract-layer brief §B lists a **secret-scanner pre-commit gate** among the B-general perimeter deliverables. The contract-layer build (2026-06-20) extended the PreToolUse egress matcher (PowerShell cmdlets + interpreter one-liners) but **scoped the secret-scanner out** — it is a *different mechanism* (git pre-commit, not PreToolUse), and bundling it would have stretched a coherent contract increment. Captured here so it is not silently dropped.
- **Proposed fix:** add a pre-commit secret-scanner hook (scan the staged diff for credential/secret patterns; fail the commit on a hit) with a **named risk + committed positive/negative fixtures** (the perimeter's own rule) and a documented wiring path; document in `core/security/hooks/README.md`. Ships to every project (core), like the egress hooks.
- **References:** `harness-v3.0/specs/memory-engine-v3-contract-layer-brief.md` §B; `core/security/hooks/block-dangerous-bash.sh` (the egress matcher extended this build); `core/security/hooks/README.md`.
- **Status:** OPEN

### v3.0-13 — Root `.gitignore` `build/` rule swallows `specs/build/` (build-spec artifacts un-stageable)
- **LANDED 2026-06-26** (this session; committed `92275bd`) — added the `!specs/build/` negation (+ a redundant belt-and-suspenders `!specs/build/**`) after the `build/` rule. Verified with `git check-ignore`: `specs/build/…` (incl. nested) tracked; `build/`, `dist/`, `apps/web/build/`, `.next/` still ignored. **Not init-touching** (gitignore only) — no G1. An adversarial workflow (`wf_125fc5c9`) caught that the contents-glob is redundant (the dir-negation alone suffices) and that the original inline comment wrongly claimed both lines were required → comment corrected to mark the glob as belt-and-suspenders.
- **Surfaced:** 2026-06-25, triaging a downstream project's `v1.1-backlog.md` (entry v1.1-1, found 2026-06-11 during Phase 0 spec commit) against the current v3.0 template — re-verified still present in v3.0.
- **Where:** `.gitignore:27` (the generic `build/` rule under "# Build outputs", lines 25-28: `dist/`, `build/`, `.next/`).
- **Severity:** standard (build-spec artifacts silently un-stageable until the operator notices; one-line workaround).
- **Symptom:** the methodology authors per-increment build specs at `specs/build/<increment-slug>.md` (`core/methodology/specs/build-spec.md.template:3`; consumed as Builder input at `core/methodology/flight-plan-template-v6.md:107`), but the depth-broad `build/` pattern (no leading slash) matches `specs/build/` at any depth, so git ignores its contents. No negation re-includes it (the only `!` rules in the 47-line file are the `.env` examples, lines 13-14); no nested `.gitignore` rescues it.
- **Proposed fix:** add a negation pair immediately after the `build/` rule — `!specs/build/` AND `!specs/build/**` (gitignore needs both the dir un-ignore and the contents glob). Prefer the negation over re-scoping the rule to `/build/` (root-only) so root/package `build` outputs stay ignored at any depth. Mechanical, reversible; **not** init-touching (gitignore only) — no G1 implication.
- **References:** downstream `v1.1-backlog.md` v1.1-1 (Surfaced 2026-06-11, Phase 0 managed-foundation spec commit; patched project-local with the same negations); current-template evidence `.gitignore:25-28`, `core/methodology/specs/build-spec.md.template:3`, `core/methodology/flight-plan-template-v6.md:107`.
- **Status:** CLOSED 2026-06-26 — landed as built in the entry's own LANDED note above.

### v3.0-14 — `init-validate` placeholder scan has no dependency/generated-dir skip list (false unresolved-placeholder failures)
- **LANDED 2026-06-26** (this session; committed `92275bd`) — added a single-sourced `dependency_scan_skip` key in `project.yaml` (read by BOTH validators via powershell-yaml + yq, **segment-matched at any depth, case-sensitive on both sides, scalar-ignored on both**), default `[node_modules, .next, .turbo, .vercel, coverage, build, dist]`; `.git` stays excluded unconditionally; schema property added (root is `additionalProperties: true`, so additive). Functional test: both validators skip `node_modules` (root + deep) and `build`, scan a capital `Build/` (case-sensitive), and catch a real placeholder in `src/` — **identically**. **G1 re-run GREEN 6/6 PS + 7/7 Unix.** **Init-touching → different-family cross-vendor verifier review OWED (ADR #1(a)), operator-fired.** An adversarial workflow (`wf_125fc5c9`; 30 findings / 6 actionable) caught + drove fixes for two real PS↔bash parity bugs the first functional test missed: PS `-contains` was case-INSENSITIVE (a `Build/` dir would skip on Windows but scan on Unix CI) → `-ccontains`; and PS honored a malformed scalar config that bash ignores → `IList` guard + an independent `project.yaml` read mirroring bash's own yq guard.
- **Surfaced:** 2026-06-25, triaging a downstream project's `v1.1-backlog.md` (entry v1.1-2, found 2026-06-12 during a Next.js scaffold validation) against the current v3.0 template — re-verified still present in v3.0.
- **Where:** `init-validate.ps1:50-51` (`Get-ChildItem -Recurse -File`, filtered only by `.git/` + `IsScanExempt`); `init-validate.sh:66` (`find ... -not -path "$SCRIPT_ROOT/.git/*"` + `is_scan_exempt`). The `placeholder_scan_exempt` list (`project.yaml`) is a META-doc exemption, not a dependency-dir skip.
- **Severity:** standard (any project that vendors deps before `init-validate` gets hundreds of false unresolved-placeholder FAILs from third-party `{{...}}` in `node_modules`/fixtures; per-project skip edits are the workaround).
- **Symptom:** after `npm install`, the recursive `{{...}}` placeholder scan walks `node_modules/`, `.next/`, `.turbo/`, `.vercel/`, `coverage/`, `build/`, `dist/` and flags third-party/template files as unresolved placeholders. The two validators do **not** diverge from each other (both skip `.git/` + the exempt list), but **neither** carries a dependency/generated-dir skip.
- **Proposed fix:** introduce ONE shared canonical skip list (`node_modules`, `.next`, `.turbo`, `.vercel`, `coverage`, `build`, `dist`, + `.git`), single-sourced so the PowerShell `Where-Object` and the Unix `find -prune`/`-not -path` consume the identical set and cannot drift. **Init-touching** — when built it takes a G1 re-run (7/7 PS + 8/8 Unix) + an ADR #1(a) different-family verifier review; low-risk since the skipped dirs aren't present in the template's permutation fixtures.
- **References:** downstream `v1.1-backlog.md` v1.1-2 (Surfaced 2026-06-12, Phase 0 Increment 1 Next.js scaffold; patched project-local across both validators); current-template evidence `init-validate.ps1:41-51`, `init-validate.sh:47-66`.
- **Status:** CLOSED 2026-06-26 — landed as built in the entry's own LANDED note above.

### v3.0-15 — Removed the mkdocs-vps deployment capability from the template
- **LANDED 2026-06-25** (this session; committed `dc2add4`) — operator-directed full strip of the `deployment` capability. See [ADR #6](adr/2026-06-25-6-remove-deployment-capability.md). Deleted `capabilities/deployment/`; removed all deployment wiring from `init.{ps1,sh}`, `init-validate.{ps1,sh}`, `project.yaml.schema.json`, `project.yaml.example`; stripped the conditional deployment steps from the `/compile` + `/audit` skills (compile steps renumbered 10/11/12 → 9/10/11); removed the `p3-deploy` G1 permutation. **G1 re-run GREEN: 6/6 PS + 7/7 Unix** (down from 7/7 + 8/8). Same-family 5-lens adversarial verifier pass cleared it (two doc orphans found + fixed: `README.md`, `ARCHITECTURE.md` slot count); different-family cross-vendor review owed per ADR #1 (a).
- **Surfaced:** 2026-06-25 — operator dogfood on the live aces instance: the mkdocs-vps docs site was machine-readable, never read, needed a separate humanizing pass, and added compile time when enabled; no value.
- **Where:** `capabilities/deployment/` (deleted); `init.{ps1,sh}`; `init-validate.{ps1,sh}`; `project.yaml.{example,schema.json}`; `capabilities/knowledge-os/extracted/{compile,audit}/SKILL.md.template`; `verifier-reviews/v2.0-item-10-run-permutations.{ps1,sh}`; `capabilities/INDEX.md`; `ARCHITECTURE.md`; `INIT.md`; `TEMPLATE-README.md`; `README.md`; `core/governance/CLAUDE.md.template`.
- **Severity:** standard (capability removal; reverses Decision V2-16).
- **Reverses:** Decision V2-16 (other deployment targets "to be authored later") — deployment is now out of template scope.
- **References:** [ADR #6](adr/2026-06-25-6-remove-deployment-capability.md); `adr/DECISIONS.md` row 6. The cosmetic `p3` permutation-label gap left in both G1 drivers is intentional (no functional impact; labels may be renumbered on a future touch).
- **Status:** CLOSED 2026-06-25 — landed as built in the entry's own LANDED note above.

### v3.0-16 — Landed the cross-vendor-verify skills (`cross-check`, `cross-check-loop`) + bridge as always-on core skills
- **LANDED 2026-06-26** (this session; committed `92275bd`) — operator-handed artifact (`cross-vendor-verify.zip`, built/hardened on the live aces instance); placed as always-on core skills per operator choice. See [ADR #7](adr/2026-06-26-7-cross-vendor-verify-core-skills.md). Added `core/skills/{cross-check,cross-check-loop}/` + the shared `core/skills/bridge/` runtime; de-acesified for the neutral template (`ACES_BRIDGE_DIR`→`CROSS_VENDOR_BRIDGE_DIR`, `aces-verify`→`claude-verify`, `aces-codex`→`codex-verify`, `aces-bridge`→`cross-vendor-verify`, aces-wiki `METHODOLOGY` refs→`core/handoffs/HANDOFF-AUTHORING.md`, migration example genericized, `DEMO.md` dropped); added the two skills to the `init-validate.{ps1,sh}` core-skill list. **G1 re-run GREEN: 6/6 PS + 7/7 Unix** (count unchanged; new skills + bridge materialize/validate across all permutations; G1 caught a literal `{{template}}` in the bridge README, fixed before green). Plus an operator-requested **preflight** in `init.{ps1,sh}` section 9: a non-fatal, presence-only check that prints at instantiation whether `node`/`codex` are present and warns the skills are INERT (not broken) until a GPT subscription is set up — verified end-to-end (init exits 0, the explicit block renders; G1 still GREEN).
- **Significance:** realizes the **rung-3 automated cross-vendor Transport** that `capabilities/decorrelated-review` (build-item 1) and the handoff-engine were blocked waiting on; answers decorrelated-review **Open Q1** (Transport lands in *this* repo). Reconciled that capability's RECIPE (§4/§6/§9).
- **Owed:** (a) different-family cross-vendor **DESIGN** review of the two skills themselves (ADR #1 (a) + the ADR #5 rung-3 gate) — the same-family 42-agent review broke+fixed but did not *clear* per break-vs-clear; operator-fired, non-Claude. (b) live `selftest.sh` run in the operator's Node env (no `node` on the build host — statically verified consistent only). (c) Codex-side mirror + repo-grounded verifier remain later separate builds.
- **Where:** `core/skills/cross-check/`, `core/skills/cross-check-loop/`, `core/skills/bridge/`; `init-validate.{ps1,sh}` (core-skill list); `init.{ps1,sh}` (preflight).
- **Severity:** standard (additive always-on capability; adds a Node + `codex`/`claude` CLI runtime prereq per project — skills fail loud without it).
- **References:** [ADR #7](adr/2026-06-26-7-cross-vendor-verify-core-skills.md); `adr/DECISIONS.md` row 7; `capabilities/decorrelated-review/RECIPE.md` (reconciled).
- **Status:** CLOSED 2026-06-26 — landed as built in the entry's own LANDED note above.

### v3.0-17 — Back-ported the formatter body-corruption engine into the knowledge-os `check-frontmatter` sensor + wired it to run
- **LANDED 2026-06-29** (this session; not yet committed) — operator-handed back-port spec + a read-only, SHA-verified extraction from the private aces-wiki. See [ADR #8](adr/2026-06-29-8-backport-frontmatter-body-corruption-sensor.md). **RTDT corrected the spec's stale premise:** the template already shipped `deploy/check-frontmatter.py` (the contract-layer derivation enforcer) — a *different* sensor — so this is a **merge, not a create/replace.** Lifted the byte-faithful body engine (`check_body` + `split_frontmatter` + helpers + `FUSE/JAM/ROW_BOUNDARY/PIPE/SEP/FENCE` regexes → TABLE-FUSE/JAM/NOSEP) + the FLATTEN detector (KNOWN_KEYS derived from the sensor's own schemas) into the existing sensor; kept the derivation/schema enforcement intact; **stdlib-only (PyYAML did NOT travel)**; WARN/degrade with `--strict` as the opt-in gate. **Closed the dormancy gap** — the contract sensor was defined + wired to `deploy/` but invoked by nothing; now run by flight-plan **Step 5.7** (knowledge-os-gated, best-effort, never-fails-the-briefing).
- **Verified:** self-test **14/14** (8 CL-3 + 5 body/FLATTEN + 1 BOM-regression, on real Python); functional smoke (detects JAM/FUSE, ignores a live table, exit 0 / `--strict` exit 1); **G1 GREEN 6/6 PS + 7/7 Unix**; adversarial workflow (`wf_c9a65334`, 27 findings / 24 confirmations / 3 nits). **Fixed** from the workflow: a pre-existing BOM gap (`_extract_frontmatter` now strips a leading BOM, else a BOM'd file skipped all frontmatter checks) + the knowledge-os RECIPE §10 migration drift (omitted the `deploy/` copy). **Left (principled):** the base-case self-test `>=` lower-bound (contract checks guard under-reporting; the FP-prone body engine uses exact `==`).
- **Did NOT travel** (instance-specific): `import yaml`, `CONTENT_DIRS` (`archery`/`dispatches`), `EXPECTED`, `KEYS_GRANDFATHERED` + entry, `raw/` BREACH, `tracked_markdown`, the docstring incident history.
- **Owed:** different-family cross-vendor verifier review of this shipped-surface change (ADR #1 (a)), operator-fired.
- **Where:** `capabilities/knowledge-os/extracted/deploy/check-frontmatter.py`; `core/skills/flight-plan/SKILL.md.template` (Step 5.7); `capabilities/knowledge-os/extracted/wiki-schema.md.template` (sensor description); `capabilities/knowledge-os/RECIPE.md` (§10 migration drift). Drift map: `harness-v3.0/backport-scope.md` (new sensor lane, row 12).
- **Severity:** standard (shipped-surface sensor capability addition + run-wiring; no `init`-mechanism change).
- **References:** [ADR #8](adr/2026-06-29-8-backport-frontmatter-body-corruption-sensor.md); `adr/DECISIONS.md` row 8; source spec `G:\My Drive\check-frontmatter-backport-to-template.md`; extraction `G:\My Drive\aces-backports\` (SHA `ae72edac…`).

---

## Records imported from the proving-ground fork's backlog (cited by docs/engine + deploy + this repo's own changelog/doctor)

`docs/engine/OPERATIONS.md`, `capabilities/knowledge-os/RECIPE.md`, and the `deploy/*.py` sensors (`compile-v2.py`, `compile-backends.py`, `check-phase-gate.py`) all cite `harness-backlog.md v3.0-20` through `v3.0-23` as the residual-tracking home for four engine limitations found during the `dogfood/aces-v3` fork's live-operations dogfood (the proving ground for the memory-engine v3 backport, W3 above). Additionally, this repo's own `HARNESS-CHANGELOG.md` (Theme D: "closes backlog v3.0-25", "v3.0-26's headline gap", "v3.0-24's W1"), `core/skills/doctor/SKILL.md`, and the self-truth brief cite `v3.0-24` through `v3.0-26` — the fork-filed template self-truth findings that W1/W2 discharged. All seven citations were **dangling in this file** — the W6 stranger test caught the class (a live drift: docs promise entries this file never carried). The seven short entries below import them so every citation resolves; full text (surfaced dates, fork commits, self-test counts) lives in the fork's own `harness-backlog.md`.

**Id-space note (read before adding the next entry after this block):** the fork's numbering is a **parallel, overlapping id-space**, not an extension of this file's own sequence — it independently occupies `v3.0-18`–`19` (dev-process findings; NOT imported here — nothing in this repo cites them) and `v3.0-20`–`26` (all imported below). To avoid a future collision if `v3.0-18`–`19` are ever imported the same way, **this file's own next native entry after this block resumes at `v3.0-27`**, not `v3.0-18`.
- **Status:** CLOSED 2026-06-29 — landed as built in the entry's own LANDED note above.

### v3.0-20 — Bridge verify-leg timeout default doesn't honor `VERIFY_TIMEOUT_MS`
- **STATUS: RESOLVED in the engine as shipped.** `BridgeVerifyBackend` hardcoded `timeout_ms=180000`, making the `VERIFY_TIMEOUT_MS` env knob inert on every backend-mediated verify leg (compile-v2 included) — a large real-content union-verify could false-timeout at ~220s regardless of the export. Fixed fork-side (`timeout_ms=None` now resolves to the env var when the caller doesn't pin one; an explicit pin still wins) and travels in this template's W3 engine backport (`944ee47`).
- **Where:** `capabilities/knowledge-os/extracted/deploy/compile-backends.py` (`BridgeVerifyBackend`).
- **Imported from:** fork `harness-backlog.md` v3.0-20 (fork commits `e38c985`, `f09f2df`).
- **Status:** OPEN

### v3.0-21 — Steady-state operations layer: registration writer, re-baseline, aging sensors
- **STATUS: PARTIALLY RESOLVED (2026-07-23, session A — template `9146c10`, cross-vendor fixes `ae52693`/`b433d4b`/`9071df4`; fork `1dd3ca2`).** The engine proves the seed but couldn't close the loop on new content — no per-intake registration writer, census breaking on the engine's own verify-leg output, no re-baseline operation, no vocabulary-aging sensors. The trusted-batch operations slice (delta registration, a shared census population boundary, journal-projected staleness behind a chain-integrity gate) was built and LOOP-1-proven on real content and travels in the W3 backport (`944ee47`; ACC-1 baseline now PENDING-tolerant). **R-1, the production session-loop registration writer, is now BUILT AND LANDED** — the harvest/stage/register/promote pipeline (`candidates.py`/`harvest-candidates.py`/`register-candidates.py`/`check-candidates.py`/`promote-candidate.py`, signing-gated promotion off a pinned SSH key, ships UNARMED by design) landed 2026-07-23 (session A of the v3.0 finishing plan; `HARNESS-CHANGELOG.md` Theme J), done-bar `drill-r1-acceptance.py --self-test` 70/70. **Residuals stay open, template-side:** re-baseline-as-an-operator-operation (still a manual, code-level act) and the ROUTE-3/LLM-4 vocabulary-aging sensors (spec'd, not built).
- **Where:** `capabilities/knowledge-os/extracted/deploy/{register-intake,staleness,check-phase-gate,candidates,harvest-candidates,register-candidates,check-candidates,promote-candidate}.py`; `docs/engine/OPERATIONS.md` § Honest gaps.
- **Imported from:** fork `harness-backlog.md` v3.0-21 (built + LOOP-1-proven `342b3bb`; backported `944ee47`). R-1 build: fork session A `1dd3ca2`, carried upstream `9146c10`.
- **Status:** OPEN

### v3.0-22 — Partial working-tree mutation on a mid-plan compile `ValidationError`
- **STATUS: RESOLVED.** `compile_v2.run()` wrote each plan item's view to the working tree as it validated, so a later item's `ValidationError` refused the whole run (nothing journaled or committed) but left earlier items' edits behind as uncommitted, dirty working-tree state — an unwary next run could stage the residue. Fixed with a two-phase run (validate every plan item first, write+commit only after all pass) plus canonicalized-key duplicate-view refusal; residuals (write-phase OSError, symlink/exotic aliases) documented as beyond-threat-model.
- **Where:** `capabilities/knowledge-os/extracted/deploy/compile-v2.py`.
- **Imported from:** fork `harness-backlog.md` v3.0-22 (fork `90cf964`, 4 sol rounds; backported `944ee47`).
- **Status:** OPEN

### v3.0-23 — Substrate-gated verify legs swallow the nested bridge verdict from accounting
- **STATUS: RESOLVED.** A `substrate-gated` outer verdict (the ambient bridge returned no attestation object) could carry a confident nested `bridge_verdict` — e.g. a real `rejected` with a full contradiction analysis — that every top-level tally (journal, ledger scorecards, `/discover` sweeps) missed, undercounting genuine rejections. Fixed by surfacing the nested verdict as a distinct `gated_inner_verdict` accounting field; the fail-closed direction was already correct (a gated verdict must never flip a candidate green) — this closed an observability gap, not a safety one.
- **Where:** `capabilities/knowledge-os/extracted/deploy/compile-backends.py`.
- **Imported from:** fork `harness-backlog.md` v3.0-23 (fork `90cf964`, 2 sol rounds; backported `944ee47`).
- **Status:** OPEN

### v3.0-24 — Template self-truth failure: every human-facing doc frozen at v2.1 while the repo is v3.0
- **STATUS: RESOLVED (2026-07-11, W1 truth reconciliation — `66dbb53`).** The 2026-07-10 stranger-test sweep found the template applying none of its own honesty machinery to itself: README/TEMPLATE-README/ARCHITECTURE/INDEX footers frozen at 2.1 against a `VERSION` of 3.0, no v3.0 changelog section, MIGRATION stopped at v2.0, a live `/grill`-vs-`/preflight` naming contradiction across 5+ files, `decorrelated-review` orphaned from init, and `check-derivation.py` self-tested but invoked by nothing. W1 reconciled all of it (see `HARNESS-CHANGELOG.md` Theme D); W5's standing self-sweep (`MAINTENANCE.md`) remains the durable guard against re-drift.
- **Where:** root docs + `capabilities/INDEX.md` + skill/capability docs beneath them (as reconciled).
- **Imported from:** fork `harness-backlog.md` v3.0-24 (surfaced 2026-07-10; the self-truth brief; resolved template main `66dbb53`).
- **Status:** OPEN

### v3.0-25 — Security hooks authored + tested and NEVER wired by init (silent); INIT.md never mentioned the manual copy step
- **STATUS: RESOLVED (2026-07-11, W1 — `66dbb53`).** `core/security/hooks/` shipped both PreToolUse guards plus `settings.local.json.example.template`, but `init.sh`/`init.ps1` contained zero wiring — every instantiated project silently ran with the bash-egress and `.env`-write guards OFF unless the operator archaeologically discovered the example file. Fixed: init §6b consent-prompted hooks wiring on both platforms (`--hooks`/`--no-hooks` flags, interactive prompt default Y, never overwrites an existing `settings.local.json`), documented in INIT.md, acceptance-verified including the `--no-hooks` consent path; `/doctor` checks the wiring thereafter.
- **Where:** `init.sh`/`init.ps1` §6b; `core/security/hooks/`; `INIT.md`.
- **Imported from:** fork `harness-backlog.md` v3.0-25 (surfaced 2026-07-10, severity blocking; resolved template main `66dbb53`).
- **Status:** OPEN

### v3.0-26 — No unified readiness check (/doctor missing); self-test coverage 3 of 18 units; codex auth never verified
- **STATUS: headline RESOLVED (2026-07-11, W2 — `54d352a` + `66dbb53`); RESIDUAL OPEN.** No single sensor could tell an operator their environment actually worked — init's end-banner was presence-only (no codex auth check, never re-run), init-validate structural-only, and every setup wall stayed invisible until a live failure. Fixed: `core/skills/doctor/` built (9 checks incl. codex auth, cross-checked via sol over 2 rounds with 2 real defects caught + fixed) and wired at init-end, `/flight-plan`, and init-validate; check 10 (`docs-stamps`) added under W5. **Residual (stays open):** broader self-test coverage of the work-doing skills — the brief measured 3 of 18 units with self-tests; not yet re-measured. Tracked under this entry, not a W-wave gap.
- **Where:** `core/skills/doctor/`; `init.sh`/`init.ps1` (end-of-init invocation); `core/skills/flight-plan/SKILL.md.template`.
- **Imported from:** fork `harness-backlog.md` v3.0-26 (surfaced 2026-07-10; headline resolved template main `54d352a` + `66dbb53`).

---
- **Status:** OPEN

### v3.0-27 — Instantiating from an untagged dev checkout carries ~280KB of maintainer history into the instance's permanent git history
- **Surfaced:** 2026-07-13, W6 stranger-test RUN 2 (pottery venture, fresh zero-context agent, fixed tree).
- **Where:** `init.sh`/`init.ps1` (strip only `capabilities/` + `core/skills/` + `VERSION` before the first commit); dev-only surfaces that survive uncopied-but-present when instantiating from a dev checkout: `BUILD-LOG.md`, `HARNESS-CHANGELOG.md`, `harness-v2.0/`, `harness-v3.0/`, `verifier-reviews/`, `handoffs/`, `adr/`, `MAINTENANCE.md`.
- **Severity:** standard (an instantiated project's git history permanently carries harness-maintainer artifacts the project operator never asked for and can't cleanly excise after the fact; not blocking — the instance itself still functions correctly).
- **Symptom:** running `init` from a git-cloned dev checkout (rather than a tagged/packaged release) commits the full dev tree state at init time; the strip list only removes template-authoring surfaces, not the maintainer-only docs, so they land in commit 1 of the new project and stay in history forever.
- **Coupling constraint:** `deploy/compile-backends.py`'s self-test reads `harness-v3.0/specs/memory-engine-v3-spec.md` directly. Stripping `harness-v3.0/` before that self-test is redirected to read the `docs/engine/` copy instead would break the self-test — any strip must land the redirect first.
- **Proposed fix:** operator decision required between (a) a release-packaging ritual that ships a trimmed artifact (tag-and-export; the dev-only dirs are never present to strip) or (b) an init-time strip-with-consent step (mirrors the §6b hooks-wiring consent pattern) that removes the dev-only dirs after redirecting the self-test dependency. See `MAINTENANCE.md` Release ritual addition.
- **References:** W6 stranger-test RUN 2 findings; `capabilities/knowledge-os/extracted/deploy/compile-backends.py` self-test.
- **RESOLVED (2026-07-24, session E, operator decision):** option (a) — the release ships as a
  **trimmed release artifact** (tag-and-export), so the dev-only dirs named above are never
  present in an instantiated project's git history in the first place; there is no strip-with-consent
  step for `init` to carry. The coupling constraint is discharged: `compile-backends.py`'s self-test
  now resolves the shipped engine-spec copy first (`docs/engine/memory-engine-v3-spec.md` post-init,
  or `capabilities/knowledge-os/extracted/engine/memory-engine-v3-spec.md` pre-init in this dev
  checkout) and falls back to the `harness-v3.0/specs/` dev path only if neither shipped copy is
  present — both layouts self-test clean (158/158). See `MAINTENANCE.md` § Trimmed-release export
  ritual for the runnable `git archive` command and its verified dry-run count.
- **Status:** CLOSED 2026-07-24 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-28 — `powershell-yaml` round-trip strips `project.yaml` comments at init stamp-back
- **Surfaced:** 2026-07-13, W6 stranger-test RUN 2.
- **Where:** `init.ps1` (the `project.yaml` stamp-back step, via `powershell-yaml`).
- **Severity:** standard (guidance text vanishes at exactly the moment a first-time operator needs it most — right after init, before the first kickoff read).
- **Symptom:** `powershell-yaml`'s read → mutate → write round-trip does not preserve comments, so every inline explanatory comment in `project.yaml.example` is silently gone from the instantiated `project.yaml` once the PowerShell init path stamps its values back. The bash path (`yq`) does not exhibit this.
- **Proposed fix:** either switch the PowerShell stamp-back to a comment-preserving patch (regex/line-based edit instead of a full parse-mutate-serialize round-trip) or emit a post-stamp notice pointing the operator at `project.yaml.example` as the annotated reference copy.
- **References:** W6 stranger-test RUN 2 findings.
- **Status:** OPEN

### v3.0-29 — Knowledge-only projects have no matching flight-plan template variant; Layer-1 field-format inconsistency
- **Surfaced:** 2026-07-13, W6 stranger-test RUN 1 (bakery venture, zero-context agent).
- **Where:** `core/methodology/flight-plan-template-v6.md` (code-build-shaped: assumes `specs/build/`, code deliverables).
- **Severity:** nice-to-have (workaroundable by hand; surfaced once, not blocking).
- **Symptom:** a knowledge-only project (no code deliverables, all content flowing through `/compile`) filling out `flight-plan-template-v6.md` hits Layer-1 field-format inconsistencies against the docs describing knowledge-os projects — the template's shape assumes a build-project's fields exist.
- **Proposed fix:** author a knowledge-only flight-plan template variant, or generalize Layer-1's field format so one template serves both project shapes without an operator inventing conventions mid-session.
- **References:** W6 stranger-test RUN 1 findings.
- **Status:** OPEN

### v3.0-30 — `/compile` roadmap cascade never fires for `scope: domain` articles
- **Surfaced:** 2026-07-13, W6 stranger-test RUN 2.
- **Where:** `capabilities/knowledge-os/extracted/compile/SKILL.md.template` Step 5 (Roadmap Cascade — fires only for `scope: build`/`scope: mixed` by design; this pass added an explicit note there so the gate is documented, not just implicit in the step's own conditional).
- **Severity:** standard (a project whose knowledge is entirely `scope: domain` gets a roadmap that never updates from compiles — silent, not loud, until an operator notices roadmap staleness).
- **Symptom:** the cascade mechanism is scope-gated on purpose (raw domain knowledge doesn't itself carry build assumptions to cascade against), and Step 5 now says so explicitly — but the underlying mechanism question stays open: *should* domain-scope content ever cascade to the roadmap (e.g. a domain fact that indirectly invalidates a roadmap assumption), or is manual reconciliation the permanent, correct answer for domain-only projects?
- **Proposed fix:** open design question, not a bug fix — revisit if/when a domain-scope-heavy project reports real roadmap-staleness pain from the manual-reconcile posture.
- **References:** W6 stranger-test RUN 2 findings; `capabilities/knowledge-os/extracted/compile/SKILL.md.template` Step 5.
- **Status:** OPEN

### v3.0-31 — Twin-build certification pilot: deferred until a small surface reaches CERTIFIED
- **Surfaced:** 2026-07-20, Theme I (behavioral-manifest layer incorporation).
- **Where:** `core/methodology/manifest-format.md` §11 (the standing obligation clause); `harness-v3.0/specs/behavioral-manifest-incorporation-brief-2026-07-20.md`.
- **Severity:** standard (a named doctrine directive with no live target yet — not blocking, but not dischargeable on request either).
- **Symptom:** doctrine directive 14 calls for an early small-surface twin-build pilot to calibrate the miss rate of the four cheap completeness hunts, but no CERTIFIED behavioral-manifest set + pinned seed exists anywhere in the program yet, so the pilot has nothing to run against.
- **Proposed fix:** deferred 2026-07-20 (named reason: no CERTIFIED behavioral-manifest set + pinned seed exists in the program). The standing obligation clause lives in `manifest-format.md` §11 — the first surface to reach CERTIFIED at small scale owes the pilot.
- **References:** `harness-v3.0/specs/behavioral-manifest-incorporation-brief-2026-07-20.md`; `core/methodology/manifest-format.md` §11.
- **DISCHARGED 2026-07-23** (session D) — the sweep-briefing format surface certified at small scale and ran the pilot: 2 rounds, 2 missing rows found in round 1 (amended §8), 0 in round 2; measured cheap-hunt miss-rate **2/18 (~11%)** folded into manifest-format.md §11 with the shared blind spot named (behavior under input conditions the source prose never described). Receipts: `receipts/sweep-briefing-conformance-bless-r1.md` + `receipts/sweep-briefing-twin-build-pilot-r1.md` (fork).
- **Status:** OPEN

### v3.0-32 — Inverted coverage: named, not built
- **Surfaced:** 2026-07-20, Theme I (behavioral-manifest layer incorporation).
- **Where:** coverage-bearing receipts (Theme I / `core/methodology/manifest-driven-builds.md`); no sensor file exists yet.
- **Severity:** standard (receipts currently imply a coverage guarantee only one direction of which is actually measured).
- **Symptom:** coverage counts rows discharged by the build; the converse — does every unit of built code discharge some row? — is unmeasured. Unobligated projection is either a missing row or code that should not exist (doctrine §10), and nothing today distinguishes the two.
- **Proposed fix:** cheapest first instrument — aggregate the per-change row citations that coverage-bearing receipts (Theme I) already require, rather than standing up a new sensor from scratch. No sensor exists; recorded so receipts stop implying it.
- **References:** `harness-v3.0/specs/behavioral-manifest-incorporation-brief-2026-07-20.md`; doctrine §10.
- **Status:** OPEN

### v3.0-33 — Doctrine v2.2 proposal set awaiting upstream ratification
- **RESOLVED 2026-07-20** — ratified under delegated authority; record:
  `harness-v3.0/specs/mdd-v22-ratification-2026-07-20.md`; all 13 decided (8 as-proposed, 5
  amended, 0 rejected).
- **Surfaced:** 2026-07-20, Theme I (behavioral-manifest layer incorporation, cross-vendor incorporation rounds).
- **Where:** `harness-v3.0/specs/behavioral-manifest-incorporation-brief-2026-07-20.md` §6; `core/methodology/manifest-format.md` (interim EXTENSIONS).
- **Severity:** nice-to-have (a forward-looking proposal queue; the harness already implements interim mechanics, so nothing is blocked on ratification).
- **Symptom:** 13 improvement proposals for doctrine v2.2 (per-proposal grounding tags `[corpus]`/`[doctrine]`/`[engine]`/`[design]`) sit recorded in the incorporation brief §6 — declared schema extensions, a canonical amendment record, row-lifecycle canonicalization, OPEN marker admission, probe-derived confidence, and LIVE/SUPERSEDED gate semantics among them — with no ratification path exercised yet.
- **Proposed fix:** ratification is operator-gated on the control-plane side, not something the harness can self-approve; the harness implements the mechanics as declared v2.1 format EXTENSIONS in `manifest-format.md` meanwhile. Fold ratified proposals in as a doctrine version bump if/when the operator ratifies v2.2 upstream.
- **References:** `harness-v3.0/specs/behavioral-manifest-incorporation-brief-2026-07-20.md` §6.
- **Status:** CLOSED 2026-07-20 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-34 — `assemble.py` top-level refusal `reason` populated only for two of six exit-1 classes
- **Surfaced:** 2026-07-20, zero-context stranger test over the manifest-gate machinery.
- **Where:** `deploy/assemble.py` (top-level refusal `reason` field, set for the behavioral-manifest gate and egress refusals only).
- **Severity:** nice-to-have (the truth is always available — itemized lines carry it — just not surfaced at the top level for every class).
- **Symptom:** the other exit-1 refusal classes — stale hard-stop, F12, taint-excluded-required, budget overflow — leave the top-level `reason` field empty; a caller reading only that field sees a bare refusal with no cause, and has to descend into the itemized lines to learn why.
- **Proposed fix:** populate `reason` uniformly across all exit-1 classes (either at the point of refusal or via a closing normalization pass before return), so every refusal is self-explanatory at the top level, not just the two classes that happen to set it today. Consistency polish, not a correctness defect.
- **References:** stranger-test finding, 2026-07-20.
- **Status:** OPEN

### v3.0-35 — `check-reference-integrity.py` is structurally blind to backtick-quoted prose paths and the onboarding docs
- **Surfaced:** 2026-07-20, zero-context stranger test — the same run that found the five dead `.template` citations fixed this session in `GLOSSARY.md`.
- **Where:** `core/governance/check-reference-integrity.py` (parses markdown link syntax only; its scan set does not include `core/onboarding/GLOSSARY.md` or `core/onboarding/TOUR.md`).
- **Severity:** standard (the sensor that exists to catch exactly this class of drift cannot see the two onboarding docs, nor the citation style they mostly use).
- **Symptom:** the sensor is structurally blind two ways at once — (a) it parses `[text](path)` markdown links, not backtick-quoted prose path citations, which is the dominant citation style in the onboarding docs; and (b) GLOSSARY.md/TOUR.md are not in its scan set at all. A stranger operating with zero context found five dead `.template` citations in GLOSSARY.md that this sensor could never have caught, on either count.
- **Proposed fix:** sensor-scope enhancement — extend the parser to recognize backtick-quoted path-shaped tokens (heuristically: contains a `/` or ends in a known extension) as reference targets, and add the onboarding docs to the scan set. Candidate for the same pass that touches the sensor next.
- **References:** stranger-test finding, 2026-07-20; this session's GLOSSARY.md fix (five `.template` citations annotated).
- **Status:** OPEN

### v3.0-36 — No capability-by-project-archetype guidance in the onboarding path
- **Surfaced:** 2026-07-20, zero-context stranger test.
- **Where:** onboarding path generally — `core/onboarding/TOUR.md`, `capabilities/INDEX.md`, `INIT.md` — no single doc maps project archetypes to recommended capability sets.
- **Severity:** nice-to-have (workaroundable by reading three docs and inferring; not blocking).
- **Symptom:** a newcomer deciding which capabilities to enable for their project has to triangulate across the Tour's capability table, `capabilities/INDEX.md`'s toggle table, and each capability's own RECIPE.md, with no single "if your project looks like X, enable Y" table anywhere in the onboarding path.
- **Proposed fix:** author a small capability-by-project-archetype guidance table (e.g. "knowledge-heavy / research project → knowledge-os on", "pure code build, no wiki → knowledge-os off", etc.) and place it in the onboarding path — Tour Stage 2 or `INIT.md`'s capability-selection step are the two natural homes.
- **References:** stranger-test finding, 2026-07-20.
- **Status:** OPEN

### v3.0-37 — Standing-loop rung 3: automated compile-on-branch with plain-English approvals (runbook Phase B)
- **Surfaced:** 2026-07-20, operator directive ("no reason to have much manual work in this harness — supercharge the you-do-the-rest part").
- **Where:** the write-side pipeline (/compile, index/health regeneration, /audit full) — currently manually invoked; the read-only side is already umbrella'd (/sweep, this same day) with a nightly-schedule recipe awaiting the operator's switch-flip.
- **Severity:** high-value (the operator is currently the scheduler of the system's own upkeep — the exact failure the harness principle forbids).
- **Symptom:** knowledge only enters the wiki when the operator remembers to run /compile; audits only run when invoked; the operator reads skill names instead of one briefing with yes/no approval moments.
- **Proposed fix:** per `harness-v3.0/specs/standing-loop-automation-brief-2026-07-20.md` rung 3 — a scheduled/triggered session that runs /compile ON A BRANCH, regenerates indexes/health, runs the full audit, and queues plain-English absorption approvals ("your note taught the wiki X and Y — absorb?"); all existing rails unchanged (append-only, census-green-or-halt, HUMAN-GATE classes, autonomy-disposition, spend envelope, kill switch). OPERATOR-GATED per the reversible-cutover runbook: eligibility gate incl. the mandatory rollback rehearsal on a scratch clone, then the Phase-B GO. R-1 stays the separate own-session component for session-end intake automation.
- **References:** `standing-loop-automation-brief-2026-07-20.md`; `aces-fork/harness-v3.0/reversible-cutover-runbook.md`; /sweep SKILL.md § Scheduling.
- **Status:** OPEN

### v3.0-38 — The decision inbox: one place where everything awaiting the operator accumulates
- **Surfaced:** 2026-07-20, operator reasoning pass on the AI-factory ideal ("all I do is make decisions and guide the workforce").
- **Where:** cross-cutting — today, items awaiting a human yes/no scatter across surfaces: sweep briefings' "Needs your attention," HUMAN-GATE authorization asks, compile-approval moments (once rung 3 lands), veto passes, backlog items marked operator-gated, GO decisions. The operator has to collect their own decision queue.
- **Severity:** high-value at factory scale (with one session a day it's tolerable; with a standing loop generating work it becomes the bottleneck interface).
- **Symptom:** no single projection answers "what is waiting on me, right now, in priority order?" — the factory's one human interface doesn't exist as a surface.
- **Proposed fix:** a DECISIONS-PENDING projection (derived, regenerated, never hand-edited — files win): every open item that needs the operator, one line each in plain English with what-happens-if-ignored and a yes/no framing, sourced mechanically from the surfaces above; folded into the flight-plan briefing's top section; cleared items flow to the normal record (raw/ADR) on decision. Design belongs with rung 3 (v3.0-37) since the compile-approval queue is its first heavy user.
- **References:** `standing-loop-automation-brief-2026-07-20.md`; v3.0-37.
- **Status:** CLOSED 2026-08-08 — built and live as the decision inbox (`deploy/decision-inbox.py` → `DECISIONS-PENDING.md`, whole-truth rework v3.0.26); only the desk-glass display of inbox ages died with the 2026-08-08 desk deletion — the inbox itself, its first-seen-age sidecar, and its pipeline are untouched.

### v3.0-39 — Desk enrichment: trends, aging, countdowns, overnight-work section, spend meter, static glass
- **Surfaced:** 2026-07-22, operator reasoning on the morning desk after the first live automated briefing.
- **Where:** /sweep briefing + decision-inbox projection.
- **Severity:** high-value at daily-use scale (the desk works; these make it trustworthy long-term).
- **Symptom:** counts without direction (the 151→165 backlog trend was noticed only incidentally); inbox items carry no age; calendar-bound items (cert expiry ~2026-08-17) show as dates, not countdowns; no "what the factory did overnight" section (matters once /standing-loop arms); automation's model spend is invisible; no passive glass surface.
- **Proposed fix:** (1) a tiny append-only metrics history the sweep writes (per-check counts per day) enabling "rising N days" trend lines; (2) inbox items stamped with first-seen dates → age display; (3) date-pattern extraction → "T-N days" countdowns for deadline-bearing items; (4) a briefing section for standing-loop overnight output + its approval queue; (5) a spend line (session count/duration since these are what's measurable locally); (6) optionally a generated static DESK.html projection in the SYSTEM-MAP visual style — a projection from the files, NEVER a second actuator (per the ARCHITECTURE bottleneck-principle corollary).
- **References:** ARCHITECTURE.md § The bottleneck principle; first live briefings 2026-07-21/22.
- **LANDED 2026-07-23** (session D, fork-first) — the `desk-metrics` recorder (40/40; append-only JSONL history, appends belong to WRITE-SIDE sessions only per the design round's correction — /sweep's read-only rule preserved, scheduled-recipe text amended in the skill) + inbox first-seen ages in `deploy/decision-inbox.py` (100/100; append-only sidecar, --check proven write-free) + countdowns wired to the v3.0-42 register (the date-scraping proposal was superseded by session C's register — recorded delta) + the `gen-desk` static-glass generator (37/37; actuator-free asserted by test: zero script/form/button). Spend line = locally measurable only (schedule-log runs + receipt token_cost).
- **Status:** CLOSED 2026-08-08 — desk DELETED from the template (operator decision, 2026-08-08 structural-audit remediation): the `desk-metrics` recorder and `gen-desk` generator removed entirely, never used or live-tested on any instance; design preserved in git history (last present at tag `v3.0.30`). The non-desk parts of this entry (inbox first-seen ages, register countdowns) live on in their own homes.

### v3.0-40 — Cross-project rollup desk (the "empire desk")
- **Surfaced:** 2026-07-22, operator: running 6-8 projects, "I'm the bottleneck in everything."
- **Where:** cross-instance — every harness project emits the same DECISIONS-PENDING.md / SWEEP-BRIEFING.md formats.
- **Severity:** nice-to-have until ≥3 instances run the standing loop; then high-value.
- **Symptom:** one desk per project re-makes the operator the aggregator across projects.
- **Proposed fix:** a rollup generator reading each registered project root's DECISIONS-PENDING.md + SWEEP-BRIEFING.md and emitting one combined page (per-project sections, waiting-on-you first, deadlines merged into one countdown list). Still a generated projection — no daemon, no CLI-calling app. Registration = a simple projects.yaml in a well-known location.
- **References:** ARCHITECTURE.md § The bottleneck principle; v3.0-39.
- **BUILT 2026-07-23, activation trigger-gated** (session D, fork-first) — the `empire-desk` rollup script (46/46; reads the session-C `projects.yaml` registry — one registry, two consumers as designed; merged waiting-on-you + soonest-first countdown + per-project sections; writes only EMPIRE-DESK.md; no daemon). ACTIVATION waits on >=2 additional live instances per this entry; nothing schedules it.
- **Status:** CLOSED 2026-08-08 — DELETED from the template (operator decision, 2026-08-08 structural-audit remediation): the ≥3-instance activation trigger never fired in the two weeks it existed; the `empire-desk` rollup script removed entirely, never dev-parked; design preserved in git history (last present at tag `v3.0.30`).

### v3.0-41 — Workspace governance: zones, birth certificates, the reaper, and the registry
- **Surfaced:** 2026-07-22, operator screenshots of the live machine (~40 root folders of repos/stale worktrees/scratch probes; 55 loose extraction screenshots) — "there needs to be some kind of governing documented apparatus."
- **Where:** above-repo filesystem — currently ungoverned by anything (the memory engine correctly does not own it).
- **Severity:** high (daily confusion cost; grows with every verification environment the method rightly creates).
- **Symptom:** good practices (QA clones, probes, evidence drives) generate artifacts with no address and no lifecycle; sessions fill placement gaps ad hoc — the doctrine's unenumerated-seams failure at the filesystem layer.
- **Proposed fix:** per `harness-v3.0/specs/workspace-governance-brief-2026-07-22.md` — four root zones (repos/worktrees/scratch/archive); WHY.md birth certificates with death dates; a report-only reaper in /sweep (deletion stays a human word); `projects.yaml` workspace registry (shared with v3.0-40's rollup desk); evidence convention `manifests/<surface>/evidence/<run-date>/` (LANDED with this entry, both repos); live-machine adoption starts with a classify-everything reap report, never a machine reorganization.
- **References:** the brief; manifest-format.md §2; v3.0-39/40.
- **LANDED 2026-07-23** (session C, fork-first) — `core/governance/WORKSPACE.md.template` (+ archive-retention fold from the seam census) + `projects.yaml.example` + the report-only reaper `deploy/check-workspace.py` (45/45 hermetic self-test incl. real-git merged/deleted-worktree fixtures; classifies zones, unregistered root entries, WHY.md required-field defects, naming-convention notes, past-lifetime folders) + /sweep step 9. Cross-vendor verified 2 rounds (gpt-5.6-sol; run dir `receipts/verify/cross-check-loop-2026-07-23/session-c-build/` on the fork) — two round-2-found defects (self-test temp leak; symbolic-HEAD-to-deleted-ref gap) fixed same-day, one settling leg owed per the escalation stub. Live-machine adoption remains operator-carried, report-first.
- **Status:** CLOSED 2026-07-23 — landed as built in the entry's own LANDED note above.

### v3.0-42 — Deadline-and-trigger register: watched rows for every time-bound promise and re-open trigger
- **Surfaced:** 2026-07-22, long-horizon seam census (the LIFETIME seam).
- **Where:** cross-cutting — prose deadlines (cert rotation ~2026-08-17; the headless-auth token expiring ~2027-07) and conditional re-open triggers (e.g. plan-8's staffAssigned free-text trigger) and PARKED items live unwatched in flight plans/ledgers.
- **Severity:** high quiet-failure value (systems dying on schedule out of well-documented prose).
- **Proposed fix:** a register (what / when-or-if / source / owner) swept nightly → countdowns + fired-trigger items in the briefing/inbox; seed it by extracting the known clocks on creation.
- **References:** `long-horizon-seam-census-2026-07-22.md`; v3.0-39 (countdown display).
- **LANDED 2026-07-23** (session C, fork-first) — `deploy/check-deadlines.py` (35/35 self-test; attention ≤30 days / fired / approximate-month / if-rows surfaced without self-evaluation / schema enforcement with owner free-form by design) + `deadline-register.yaml.example`; the fork's real register seeded with the corpus-grounded 2026-08-17 cert-rotation clock (already reporting 25 days at landing), the staffAssigned if-trigger, and the ~2027-07 clock honestly census-cited (no corpus source states it — row says re-derive). /sweep step 10. Cross-vendor CONFIRMED/confident round 2 (same run dir as v3.0-41). Countdown display in the desk stays v3.0-39 (session D).
- **Status:** CLOSED 2026-07-23 — landed as built in the entry's own LANDED note above.

### v3.0-43 — Drift sensors for the harness's own substrate: toolchain versions + mirror/instance parity
- **Surfaced:** 2026-07-22, seam census (VERSION + PARITY seams; both lived on 2026-07-21/22: CLI behavior changed across a mid-program update; mirror byte-identity is only ever proven at sync time).
- **Proposed fix:** (a) environment manifest (tool / version-last-verified / date) + doctor drift flags — the reserved dependency layer pointed at the harness itself; (b) sweep parity report — engine-mirror hashes fork↔template, per-instance skill inventory vs template HEAD; doubles as an empire-desk column.
- **References:** the census; doctor check 2 (presence-only today).
- **LANDED 2026-07-23** (session C, fork-first) — (a) doctor `check_version_drift` (40/40 self-test incl. the absent-PyYAML branch; unreachable-probe WARN names the recorded version + verified date) + `environment-manifest.yaml.example`; the fork's manifest seeded with live-probed versions, all four PASS on the fresh run; doctor instantiated onto the fork byte-identically — the census's own PARITY example remediated. (b) `deploy/check-parity.py` (18/18 self-test; SHA-256 over engine mirror + skill inventory; instance-config and .example/.template pairs informational): its first real run surfaced 6 genuine pre-existing drift findings (conformance SKILL.md, two preflight docs, gen-clearance-claims.py never mirrored, descriptors/README.md never pulled, orient never instantiated) — operator's queue, not silently fixed. /sweep step 11. Cross-vendor 2 rounds; residuals are evidence-precision only, per the escalation stub.
- **Status:** CLOSED 2026-07-23 — landed as built in the entry's own LANDED note above.

### v3.0-44 — Behavioral manifests for the harness's own model-facing surfaces (the dogfood)
- **Surfaced:** 2026-07-22, seam census (FORMAT seam — briefing/inbox/receipt forms are contracts between model-authored text and parsers; one drift instance already caught and guarded ad hoc).
- **Proposed fix:** enumerate the sweep-briefing / DECISIONS-PENDING / receipt formats as behavioral-manifest rows under `manifests/harness-surfaces/`; /conformance replays them; MDD applied to MDD's own machinery — also the natural first CERTIFIED small surface, which would trigger the standing twin-build pilot obligation (v3.0-31).
- **References:** the census; manifest-format.md; v3.0-31.
- **LANDED 2026-07-23** (session D, fork-first) — new `format` layer admitted to `manifest-layers.yaml` per its own admission rule; three surfaces under per-surface dirs: `manifests/sweep-briefing/` **CERTIFIED** (18 rows: 13 validator + 5 RUBRIC; validator `deploy/check-briefing-format.py` with per-row single-defect seed isolation; named smoke subset) + `manifests/decision-inbox/` and `manifests/compile-receipt/` EXTRACTED with DRAFT rows. The dogfood's first catch landed before certification: the grading pass exposed /sweep SKILL.md contradicting its own skeleton on check-name vocabulary — contract reconciled both repos. Certification receipt + the twin-build pilot (see v3.0-31 discharge) beside it. Manifest sets carried template-side under `capabilities/knowledge-os/extracted/manifests/` (pins resolve on instances, same convention as extracted/deploy). Extraction also surfaced a real corpus contract fork: compile-receipt `scope_tags`/`cross_links_changed` have two competing live shapes (61 vs 15 files) — held as OPEN rows for that surface's future certification.
- **Status:** CLOSED 2026-07-23 — landed as built in the entry's own LANDED note above.

### v3.0-45 — Content-layer /compile and the mechanical-engine census disagree on "done" state, with no signpost
- **Surfaced:** stranger-test RUN 1 (2026-07-24) — a zero-prior-context operator instantiating the harness for a woodworking bench-plans venture, completing a first knowledge-capture/compile cycle per the task's ask.
- **Where:** `.claude/skills/compile/SKILL.md` (content-layer compile protocol, `sources:` back-reference); `capabilities/knowledge-os/extracted/deploy/staleness.py` (conservation census — UNROUTED vs. CONSUMED classification); `docs/engine/OPERATIONS.md` (the mechanical pipeline: register-intake → routing/census → compile-plan → HUMAN-GATE → compile-backends absorb → cross-vendor VERIFY → census-green).
- **Severity:** standard (no data lost or misrepresented; operator has to independently discover that the two "done" surfaces don't agree, and reconcile by judgment).
- **Symptom:** the operator followed `.claude/skills/compile/SKILL.md`'s content-layer protocol faithfully and produced a fully schema-conformant wiki article with a correct `sources:` back-reference. But `deploy/staleness.py --report`'s conservation census still classified the underlying raw event as **`UNROUTED`**, not `CONSUMED` — the engine layer (`register-intake.py` → routing/census → compile-plan → HUMAN-GATE → `compile-backends` absorb → cross-vendor VERIFY → census-green, per `docs/engine/OPERATIONS.md`) keeps its own registration/entities/derivation-block bookkeeping, entirely separate from the content-layer's plain `sources:` list — finishing the content-layer protocol does not, and cannot, make the engine's own view show the event as consumed. The compile `SKILL.md` gestures at this split ("the mechanical pipeline... does not replace this skill") but doesn't warn that a fully-compiled article will still show its source event as `UNROUTED` residue in the engine's own health/census view — a state `docs/engine/OPERATIONS.md` step 3 itself calls "not a defect," so nothing is actually broken, but the two layers' "done" states visibly disagree with no signpost warning a first-time operator that this is expected.
- **Proposed fix:** not designed here (deferred pending triage) — candidates include: (a) `.claude/skills/compile/SKILL.md` explicitly states, at the point it declares the content-layer compile complete, that the engine's own conservation census will still show the source event `UNROUTED` (or `PENDING`) until the separate mechanical ABSORB/cross-vendor-VERIFY loop runs, with a pointer to `docs/engine/OPERATIONS.md`; (b) reconsider whether "UNROUTED" is the right word for an event whose content-layer compile is already done — it currently reads identically to "never touched."
- **References:** `harness-v3.0/stranger-test-2026-07-24/RUN-1-FINDINGS.md`, Finding 6.
- **Status:** OPEN

### v3.0-46 — `check-loop-state.py --self-test` not long-path-robust on deep Windows destinations
- **Surfaced:** stranger-test RUN 2 (2026-07-24) — a zero-prior-context operator instantiating the harness for a coffee-roasting subscription venture, running `/doctor`'s full sensor suite against a fixed, pre-assigned deep destination path (the target path could not be shortened; the exercise fixed it).
- **Where:** `deploy/check-loop-state.py --self-test` (invoked by `/doctor`'s sensor suite); its bundled fixtures under `deploy/test-fixtures/loop-state/trees/...` (e.g. `invalid-unregistered-informed-by/raw/2026-07-01-ryan-fixture-decision-lock.md`).
- **Severity:** friction (does not block the project — `/doctor` surfaces this as a FAIL every run at this path depth, but nothing downstream depends on the self-test passing; the operator cannot fix it themselves without editing harness-shipped sensor code, which is out of scope for a fresh operator).
- **Symptom:** the sensor's `--self-test` copies its own bundled fixture files into a scratch temp directory. At this project's real (non-junctioned) destination path, the fixture source path is 264 characters — over Windows' `MAX_PATH` — so the copy fails with `[WinError 3] The system cannot find the path specified`. This is independent of git and independent of the directory-junction workaround used to get `init.ps1` itself running (v3.0-… / stranger-test RUN 2 Finding 1's init.ps1 pre-flight guard): doctor was invoked with a plain `python ...\doctor.py <real long path>` argument, and the sensor's own path handling re-derives the real path rather than staying inside any short alias. `/doctor`'s FIX line ("run the self-test directly and fix the failing cases") is maintainer-facing guidance aimed at patching Python, not something a new operator is equipped to act on — doctor reports this FAIL in every run for the life of an instance at this path depth. Operator-side workaround: instantiate at (or access via a directory junction to) a short path, the same workaround as Finding 1 — this also sidesteps the self-test's own path-length ceiling.
- **Proposed fix:** not designed here (deferred pending triage) — belongs in the sensor's fixture-path handling, e.g.: (a) detect a self-test copy failure caused specifically by path length and report it as a distinct, clearly-labeled "environment path too deep" condition instead of a raw WinError; (b) shorten the bundled fixture tree's own nesting depth so the self-test's fixed path overhead is smaller; (c) have the self-test stage its scratch copy under a short temp-root alias regardless of where the source tree lives.
- **References:** `harness-v3.0/stranger-test-2026-07-24/RUN-2-FINDINGS.md`, Finding 3.
- **Status:** OPEN

### v3.0-47 — No raw session/tool-trace persistence; pilot isolation is prompt-enforced, not sandbox-enforced (verification legs keep hitting the same evidence ceiling)
- **Surfaced:** session D build /cross-check-loop (2026-07-23, `receipts/verify/cross-check-loop-2026-07-23/session-d-build/escalation.md`, residual (a)); operator-dispositioned session E (2026-07-24) as a forward-looking capability filing so the class stops recurring per-session.
- **Where:** the hub-transcription mechanism that produces agent FINAL MESSAGES (labeled as such) in place of raw immutable session/tool logs; the absence of a worktree/sandbox harness for render legs, leaving pilot isolation prompt-enforced.
- **Severity:** standard (contests no shipped behavior; every affected receipt already labels the limitation honestly — the gap is that verification cannot independently confirm what a hub-transcribed summary claims happened, and cannot structurally prove isolation held).
- **Symptom:** cross-vendor verifiers repeatedly ask for original session/tool traces to check grader and render legs, and for structural (not prompted) proof that pilot runs were isolated, and the harness has neither to hand — this is a structural property of the substrate, not of any one build, and the identical residual class has now recurred across sessions B, C, and D's escalation stubs, each time re-argued and re-accepted from scratch.
- **Proposed fix:** not designed here (deferred pending triage) — candidates: (a) persist raw session/tool traces alongside hub-transcribed final messages, at minimum for verification-relevant legs, so a verifier can check the transcript rather than trust the summary; (b) a worktree/sandbox harness for render legs so pilot isolation is enforced structurally (e.g. filesystem/process boundary) rather than by prompt instruction alone. Either would let future cross-check-loop rounds close this residual class instead of re-filing it.
- **References:** `receipts/verify/cross-check-loop-2026-07-23/session-d-build/escalation.md` residual (a); the same residual class in the session B and session C escalation stubs.
- **Status:** OPEN

### v3.0-48 — `check-reference-integrity.py`'s Phase-1 scoping (markdown links only, default doc = root CLAUDE.md) leaves inline-code path refs and root docs unchecked
- **Surfaced:** 2026-07-25, fork doc-truth fix pass (ARCHITECTURE.md, README.md, CONTEXT.md) — three dangling `.claude/skills/grill/` inline-code references, stale since the 2026-07-09 grill→preflight rename, sitting undetected in the fork's root docs.
- **Where:** `core/governance/check-reference-integrity.py` — its own docstring admits the gap: lines ~13-14 ("Source: markdown link targets `](path)` in the COMMITTED governing doc(s) ... Only link targets in PROSE are checked") and line ~36 ("The dangling reference that prompted the question was also inline-code, which this sensor never checks — only markdown link targets"). Default doc set is `core/governance/CLAUDE.md` plus `project.yaml.governance_docs[]`; root docs (`ARCHITECTURE.md`, `README.md`, `CONTEXT.md`) are not in that declared set unless a project adds them.
- **Severity:** standard (no data lost; the sensor behaves exactly as documented, but its documented scope leaves a real, demonstrated blind spot — three stale references sat unnoticed through the entire grill→preflight rename and every subsequent flight-plan run).
- **Symptom:** a path-shaped reference written as inline code (`` `.claude/skills/grill/LICENSE-mattpocock.txt` ``) rather than a markdown link (`[text](path)`) is invisible to the sensor even when it points at a file that no longer exists, and even in a doc the sensor never scans in the first place (root docs outside the declared governing set). The rename that orphaned these three refs happened 2026-07-09; the sensor has run every session since via `/flight-plan` Step 5.6 and never flagged them, because both the reference form (inline code) and the doc location (fork root, not `core/governance/CLAUDE.md` or a declared `governance_docs[]` entry) sit outside Phase 1's scope.
- **Proposed fix:** not designed here (deferred pending triage) — this is a design decision (false-positive budget), not a bug fix, since the current scoping was deliberate (§ Scope, "the high-signal subset is repo-relative markdown link targets: canonical refs are links; examples are plain/inline-code text"). Candidates: (a) extend matching to inline-code path-shaped refs (`` `path/to/thing` ``) with an explicit ignore convention for deliberate illustrative examples, so real dangling refs written as inline code stop passing silently; (b) widen the default/declared doc set to include root-level docs (`ARCHITECTURE.md`, `README.md`, `CONTEXT.md`) rather than requiring each project to opt them into `governance_docs[]`. Either change trades some false-positive risk (inline code is used for non-path examples too) for closing a demonstrated blind spot — worth an explicit call, not a silent expansion.
- **References:** `core/governance/check-reference-integrity.py` docstring lines ~13-14, ~36; fork fix commit-pending in `ARCHITECTURE.md`, `README.md`, `CONTEXT.md` (aces-fork, 2026-07-25).
- **Status:** OPEN

### v3.0-49 — Generation-lane depth unguarded: first-draft generative steps carried no refusable tests (the kickoff-glossary thinness class); Passes 3 and 5 of the five-pass method had no forcing function anywhere in the harness
- **Surfaced:** 2026-07-26, fork `dogfood/aces-v3` session, adjudicating a live instantiation's kickoff transcript (2026-07-25): Step 2h produced a generic, landscape-only glossary that only operator rejection ("this seems thin") deepened — while Step 2i, which carries Pocock's three mechanical criteria, stayed sharp unprompted. Second instance of the class already on record: the v3.0-21 defect-class generalization came from a totality pass the operator had to request. Source material: the operator's five-pass research method (personal methodology note, 2026-02-23).
- **Where:** `INIT.md` Steps 2f–2i (list-producing, test-less except 2i); `core/skills/preflight/SKILL.md` (passes 2+4 codified, pass 3 absent); no `core/methodology/` doc carried the research/reasoning discipline (`flight-plan-template-v6.md` covers execution only).
- **Severity:** standard (quality, not honesty — nothing false ships; but every thinness catch spent operator attention, the scarce budget, and un-prodded instances ship thin first drafts as anchors).
- **Symptom / defect class:** the harness guards **finished claims** with heavy machinery (cross-check, handoffs, conformance, sensors) and **first drafts** with nothing. Instruction-shaped depth ("reason about the totality") decays into checkbox theater; only refusable artifacts survive. Pocock's ADR criteria are the existence proof — a codified Break Template pass that a candidate can *fail* — and the glossary step was the counterexample: `CONTEXT-FORMAT.md` already carried the "only project-specific terms" rule and the `_Avoid_:` convention as buried prose, and the thin draft happened anyway. Design criterion extracted: a codified pass must cost more to fake than to run; codify what output must survive, never the order of thought.
- **RESOLVED 2026-07-26 (this commit, tag v3.0.6):** `core/methodology/five-pass-method.md` (the five passes + the refusability doctrine + pass-to-home table); `INIT.md` — show-the-rejects convention for 2f–2i (empty reject pile on a first draft = thinness tripwire), 2h three-leg glossary term test (project-specific / behavior-changing / displaces-a-named-near-miss), new Step 2j totality read-back with explicit no-findings attestation, post-interview validation extended (per-term `_Avoid_:` lines; 2j section present); `core/skills/preflight/SKILL.md` § 4 unconstrained-ideal note (skippable only with recorded reason); `core/methodology/INDEX.md` + `ARCHITECTURE.md` enumeration updated. Fork mirrors byte-identical.
- **DEFERRED (named forward capabilities, deliberately not built):** generic-vocabulary sensor over glossary drafts (sensor-class sibling of the vocabulary-aging family, ROUTE-3/LLM-4); pass-payoff telemetry (how often a codified pass changed an outcome — the empirical liturgy-pruner); depth-grading as a standing dimension on cross-vendor legs. Until built, independent thinness checks ride the existing `/cross-check` bridge as ordinary falsifiable claims (e.g. "no term in this glossary would survive unchanged in a neighboring project") — no new verifier machinery.
- **References:** `core/methodology/five-pass-method.md` (lineage footer); fork session 2026-07-26; the instantiation kickoff log that surfaced the class (external instance, 2026-07-25); backlog v3.0-21 (the operator-requested totality pass precedent).
- **Status:** CLOSED 2026-07-26 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-50 — Substrate-plural doctrine, Claude-only orientation wiring: non-Claude agent sessions receive zero governance (no AGENTS.md; CLAUDE.md phrased Claude-only; security hooks silently don't travel)
- **Surfaced:** 2026-07-26, operator, from a live Codex working session on an external instance: Codex verified no `AGENTS.md` exists anywhere in the tree, so it never received the session contract and had to be pointed at `core/governance/CLAUDE.md` by hand — with the operator naming the general defect: "this harness should not assume the operator is only going to use Claude, especially when one of our skills requires a ChatGPT subscription."
- **Where:** template root (no `AGENTS.md`; grep template-wide = zero hits); `core/governance/CLAUDE.md.template` lines 6 and ~117 ("Every Claude Code session..."); `INIT.md` kickoff framing ("Open Claude in a fresh chat"); `core/security/` (PreToolUse hooks wire into `.claude/settings.local.json`, which only Claude Code reads — no statement anywhere that other agents fall outside the perimeter).
- **Severity:** standard-but-important (governance gap, not data loss — but the harness's own substrate-separation doctrine *requires* non-Claude sessions: `/cross-check` runs on a codex subscription, every handoff verifier is cross-vendor. The verifier path masked the gap because handoff packets are self-contained by design; the unmasked case is a non-Claude agent as a *working* session, now demonstrated live).
- **Symptom / defect class:** an agent whose tool loads `AGENTS.md` by convention (Codex, and the growing set of tools honoring that convention) starts with zero orientation: no session discipline, no directory-preservation or single-writer rules, no decision-lock firewall, no glossary — and no warning that the Claude-Code-scoped hook perimeter does not mediate its tool calls (the v3.0.1 matcher-scope lesson, one level up: a perimeter only guards the tools it's wired to).
- **RESOLVED 2026-07-26 (this commit, tag v3.0.7):** root `AGENTS.md` ships — a **pointer, not a copy** (single-home rule): reading order (CLAUDE.md → PROJECT-COMPASS → HARDCONSTRAINTS → CONTEXT.md → current flight plan) plus the five conventions that most trip up unoriented sessions, including the explicit hooks-don't-travel warning and the record-your-real-substrate rule. `CLAUDE.md.template` both Claude-only lines neutralized (contract is substrate-neutral; filename stays — see rejects); `INIT.md` kickoff framing de-Claude-ed; `TEMPLATE-README.md` canonical-paths table + `ARCHITECTURE.md` § governance updated. Fork counterparts applied (root `AGENTS.md` copied; instantiated `core/governance/CLAUDE.md` same two lines).
- **Rejects (considered and killed):** renaming `core/governance/CLAUDE.md` to a substrate-neutral name (tree-wide reference churn for zero contract change; rename stays possible later — nothing foreclosed); duplicating governance content into `AGENTS.md` (drift — the exact failure mode the single-home rule exists for); per-vendor pointer files (`GEMINI.md`, etc. — `AGENTS.md` is the emerging cross-tool convention and one pointer is enough).
- **DEFERRED (named forward capabilities):** security-perimeter parity for non-Claude agents (e.g. a shipped Codex `config.toml` sandbox/approval counterpart to the hook wiring — real work, needs its own design pass); `/doctor` check for root `AGENTS.md` presence + pointer-target existence (cheap, next sensor batch); `AGENTS.md` inherits the v3.0-48 root-doc scanning blind spot until that entry's triage.
- **References:** operator report 2026-07-26 (Codex session transcript, external instance); `core/security/hooks/README.md` § Matcher scope (the in-family precedent); v3.0.1(a) (the same lesson at hook-matcher level).
- **Status:** CLOSED 2026-07-26 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-51 — `compile: false` is write-once and never audited: no sensor ever re-checks that a flagged file's knowledge has a canonical home (live instance found with 19/20 raw files flagged and a zero-article wiki)
- **RESOLVED (core) 2026-07-27 (tag v3.0.8):** `deploy/check-knowledge-debt.py` ships per the revised design — date-gated `canonical:` enforcement (cut 2026-07-27; legacy WARN-grandfathered; dangling or repo-escaping targets FAIL at any age), wired into flight-plan Step 5.10 (every session) + `/sweep` structural sensors; schema § 2 documents the field with `deliverables/` explicitly legitimate. Self-test 7/7; two-round gpt-5.6-sol leg (fork `receipts/verify/knowledge-debt-2026-07-27/`) — round 1 caught a REAL path-escape acceptance bug + a permission-masquerade exit code, both fixed and fixture-locked; round-2 residuals accepted-class. Live proof: the audited v1.1 instance's corpus reports 19 legacy-unannotated honestly. **RESIDUALS (stay open here):** the HEALTH.md honesty line (rides the next compile-engine change, briefing coverage carries it meanwhile); per-instance corpus adjudication (operator-gated by design); registration-layer disposition upgrade path (v3.0-21 residuals).
- **Surfaced:** 2026-07-27, operator-requested once-over of an external v1.1 instance (named in FLEET-LEDGER.md, dev-only): 19 of 20 raw files carry `compile: false` — including plainly-knowledge-intake research (permit-path, insurance, tax-election, equipment, regulatory-corpus files) — the wiki has zero compiled articles, and `/compile` ran exactly once (2026-06-08, operator answered "skip them all" at the scope gate) and never again. Sessions routed synthesized outputs to an un-governed `deliverables/` directory and used `compile: false` to keep the pipeline quiet — articulate and deliberate, but schema-contrary (the schema's own line: research findings COMPILE; process records archive). The single `compile: true` file has been stuck for over a month on a dependency cluster of `compile: false` base files; its recurring REVIEW.md re-detection is the only tripwire that ever fired.
- **Where:** `docs/wiki-schema.md` § 2 (`compile: false` defined; nothing re-audits it); `/compile` (INFO-level skip, no canonical-home check); `/audit` (grades roadmap vs wiki, blind to raw-side flags); `/sweep` + `/flight-plan` (count `compile: true` unprocessed only — flagged files are invisible by design).
- **Severity:** standard-but-important (silent knowledge-pipeline-shutdown class: the flag makes an empty wiki look intentional to every sensor; the compounding-knowledge machinery idles at 100% with all lights green except one background REVIEW entry).
- **Symptom / defect class:** same family as v3.0-49/v3.0-50 — a declared-but-never-verified claim. `compile: false` asserts "canonical output already lives elsewhere"; nothing ever checks where, or whether it exists. Cheap countable signals exist and go unused: wiki article count vs flagged-file count; canonical home named in frontmatter or not.
- **Proposed fix (deferred; REVISED 2026-07-27 by an operator-requested five-pass reasoning run over the original proposal):** (a) schema: `compile: false` gains a `canonical:` field naming the home (a path — `deliverables/` explicitly VALID, legitimizing the corrected-deliverable-as-truth pattern instead of fighting it — or a `docs/adr/`/governance reference); **date-gated per the check-loop-state precedent**: required on files created after the cut date, WARN-historical on legacy files (append-only rule means retroactive requirement would break every existing instance forever); (b) the check is a **standalone read-only sensor wired into `/flight-plan` (every session) and `/sweep` (cadence)** — NOT compile-only, because a compile-time WARN guards a pipeline nobody runs (the audited v1.1 instance ran /compile once, ever); verifies field presence (date-gated severity) + target existence; self-labels as pointer-integrity, not content-coverage (an existing target is a proxy, not proof the knowledge is in it); design pass must weigh reusing `check-reference-integrity.py` machinery vs a third bespoke path-scanner (v3.0-48 neighborhood — avoid sensor sprawl); (c) honesty line in **both** `HEALTH.md` and the flight-plan briefing ("N archived compile:false — M homes verified, K legacy-unannotated") — HEALTH alone rusts, it is regenerated *by* the pipeline whose silence is the failure mode. Sensor reports, never flips flags; corpus adjudication stays operator-gated. **Named upgrade path (do not foreclose):** the frontmatter field is the portable precursor of a registration-layer disposition (compiled / archived-with-home / pending) counted by the conservation census — lands with the v3.0-21 steady-state residuals; the two must not diverge. Original (pre-revision) proposal kept in git history; rejects from the reasoning run: sensor-only heuristics with no field (can't distinguish governed archive from shadow wiki), abolishing compile:false (process records are real), compile-scope-gate process rules alone (doesn't survive the operator answering "skip all").
- **References:** the v1.1-instance once-over 2026-07-27 (operator session, fork-side); that instance's `wiki/REVIEW.md` 2026-06-08 entry (the stuck dependency-cluster dangler, stale since 2026-06-22); sibling defect split out as v3.0-52 (stale REVIEW entries never escalate — the tripwire that fired here and rusted).
- **Status:** CLOSED 2026-07-27 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-52 — REVIEW.md entries have stale-after dates but nothing ever escalates a stale entry: the one tripwire that fired in the v1.1-instance shutdown rusted in place for a month
- **RESOLVED 2026-07-27 (tag v3.0.8, same sensor as v3.0-51):** `check-knowledge-debt.py`'s REVIEW class — report-only, never FAILs; entries past stale-after surfaced with ages (30d+ escalates to the briefing's Attention Needed), undated stale lines counted rather than silently skipped. First live measurement quantified the debt the class had been hiding: aces-fork 98 entries — 54 stale (oldest 93d), 36 undated. Clearing stays governed by the existing compaction rules; the sensor only ends the silence. **Entry closed.**
- **Surfaced:** 2026-07-27, fell out of the five-pass reasoning run over v3.0-51's fix: the v1.1 instance's `/compile` skip left a background-severity REVIEW entry dated 2026-06-08 with `Stale after: 2026-06-22` — the entry did its job, went stale, and no sensor, briefing line, or skill ever surfaced "a REVIEW entry is past its stale date." It sat rusted for over a month while every session's lights stayed green. Independent of the compile:false defect: stale-REVIEW escalation alone would have re-surfaced the shutdown.
- **Where:** `docs/wiki-schema.md` REVIEW lifecycle (defines stale-after windows per severity; defines creation and compaction-clearing; defines no escalation); `/flight-plan` (briefs unprocessed counts, not REVIEW staleness); `/sweep`, `/audit` (neither walks REVIEW ages).
- **Severity:** standard (observability; fail direction is silence, not falsehood).
- **Proposed fix (deferred, likely rides the same sensor as v3.0-51b):** a read-only check that lists REVIEW entries past their stale-after date, surfaced in the flight-plan briefing as aging debt ("K review entries stale, oldest N days") — report-only; clearing stays governed by the existing compaction rules.
- **References:** the v1.1 instance's `wiki/REVIEW.md` (the 2026-06-08 entry, stale 2026-06-22, still present 2026-07-27); v3.0-51 (the reasoning run that split this out).
- **Status:** CLOSED 2026-07-27 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-53 — REVIEW.md is a stored-flag system in a re-derive-everything harness: no lifecycle beyond an unrun "compaction" clause, writers emit unparseable stale lines, and stale means expired-observation, not overdue-task
- **Surfaced:** 2026-07-27, operator-requested five-pass run over "the review solution" (follow-on to v3.0-52's observability patch; live corpus: aces-fork 98 entries — 54 past stale-date, 36 undated). Key break-template findings: (1) stale-after is a FRESHNESS window ("don't trust after this date"), not a deadline — escalating stale entries as overdue tasks is the wrong half of the fix; (2) REVIEW stores flags while every sibling surface re-derives every run ("no stored flag" is flight-plan's own language) — the 98-pile is what stored flags do at volume; (3) the queue conflates sensor-re-derivable observations (stored copy redundant) with human-judgment items (belong in the decision inbox, which is where decisions reach the operator).
- **Proposed direction (deferred, needs design pass):** shrink what gets stored rather than manage the pile — (a) sensor-backed entry types: expiry-equals-deletion at stale date, safe BECAUSE re-derivation re-raises persisting problems with fresh dates; (b) judgment-needed types route to DECISIONS-PENDING/decision-inbox at creation, REVIEW keeps a pointer; (c) writer-side validation: new entries must carry parseable stale-after (cheap date-gated extension of check-knowledge-debt); (d) compaction becomes a real runnable operation in the standing-loop janitorial cycle. Unconstrained ideal (upgrade path, not now): REVIEW as a generated view like HEALTH. Instance work, separate: one-time operator-gated triage of the aces 98 using the sensor's list as worklist.
- **slice (c) RESOLVED 2026-07-27 (tag v3.0.10):** writer-side validation shipped — `check-knowledge-debt.py`'s REVIEW class now FAILs a NEW entry (Created >= 2026-07-27) whose stale-after is unparseable (a structural writer defect: it could never age out), while aging stays report-only and legacy undated entries stay grandfathered WARNs; self-test 7/7 w/ new fixture. **Slices (a) expiry-equals-deletion, (b) inbox routing, (d) runnable compaction remain open — design pass still owed.**
- **References:** five-pass run 2026-07-27 (this session); v3.0-52 (observability layer, shipped v3.0.8); wiki-schema §8/§12 (the compaction clause nobody executes).
- **Status:** OPEN

### v3.0-54 — Artifact classes without declared homes: deliverables/ (instance-invented, now cited by v3.0.8's own schema text WITHOUT a definition), large-media assets, design briefs; instances invent shadow systems exactly where the schema is silent
- **Surfaced:** 2026-07-27, operator-requested five-pass run ("does everything the harness creates have a dedicated folder path?"). Landscape: raw/wiki/flight-plans/references/adr/roadmap/handoffs/receipts/manifests all declared and governed. Three classes are NOT: operator-facing synthesized outputs (the audited v1.1 instance INVENTED deliverables/ for its synthesized operator artifacts — insurance map, filled regulatory forms — the same instance that grew the compile:false shadow wiki, and for the same reason: the schema had no home to offer); large/binary media assets (the music-video instance's next gate is literally an asset-intake manifest because nothing says where assets live); design briefs/specs (dev-repo convention, no instance home). SELF-CAUGHT URGENT SLICE: v3.0.8's schema §2 canonical: paragraph names deliverables/ as a legitimate home — and no schema section defines deliverables/. We shipped a pointer into undeclared territory; the later pass kills the earlier one's oversight.
- **Proposed fix (deferred except (a), which should ride the next patch):** (a) define deliverables/ in wiki-schema — operator-facing synthesized outputs incl. binaries/filled forms, each carrying a derived-from provenance note; init scaffolds deliverables/README.md (the references/ precedent, v3.0.4c); closes the dangling v3.0.8 citation; (b) assets/ convention for large media: repo holds pointer-manifests only (external location + SHA-256 + provenance), never binaries; first consumer is the music-video instance's asset-intake gate; (c) junk-drawer tripwire in /sweep: tracked top-level dirs not on the declared list flagged informationally — the generalized sensor for the whole instances-invent-where-schema-is-silent class. Rejects: full project.yaml artifact registry (config sprawl for v1 benefit; named as upgrade path); forcing binaries into the wiki.
- **RESOLVED 2026-07-27 (tag v3.0.10):** (a) `deliverables/` defined in wiki-schema § Artifact homes (derived, provenance-noted, canonical-target-eligible, not knowledge intake) + both init scripts scaffold `deliverables/README.md` (references/ precedent) — the v3.0.8 dangling citation is closed; (b) `assets/` declared: pointer-manifests (`assets/<slug>.asset.md` — location, SHA-256, size, provenance/rights) never binaries, first consumer the music-video instance's asset-intake gate; (c) `/sweep` step 12 top-level layout honesty check (tracked dirs vs documented layout, informational-never-blocking, unrecognized dir = artifact class needing a home). **Residual:** full project.yaml artifact registry stays the named upgrade path, not built.
- **Totality note (the trilogy):** v3.0-51 (unverified claim), v3.0-53 (unlifecycled store), v3.0-54 (undeclared homes) are one meta-defect — the harness governs what it imagined and stays silent about what instances invent. One instance invented deliverables/; the music-video instance is about to invent assets/; every invention becomes a shadow system until adopted.
- **References:** five-pass run 2026-07-27; the v1.1-instance once-over (deliverables/ inventory); v3.0.8 schema text (the dangling deliverables/ citation); the music-video instance's kickoff (asset-intake gate pending).
- **Status:** CLOSED 2026-07-27 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-57 — Orientation asymmetry inverted by v3.0.7: Codex auto-loads AGENTS.md every session while Claude Code auto-loads root CLAUDE.md — which the harness never shipped (contract at a path Claude Code's tooling never reads unprompted)
- **Surfaced:** 2026-07-27, operator observation: "codex runs the harness machinery a lot cleaner than claude... auto compile and record decisions after major turns a lot more thoroughly." Diagnosis: structural, not (only) behavioral — since v3.0.7, a Codex session starts every turn pre-briefed by root `AGENTS.md` (reading order + the raw/compile + decision conventions), while a Claude Code session starts cold: the contract lives at `core/governance/CLAUDE.md`, which Claude Code does not auto-load, and no root `CLAUDE.md` existed. The dogfood fork confirmed it live: sessions there launched with zero project contract in context. v3.0-50's fix created its own mirror image — the harness now auto-oriented every agent EXCEPT the one it was built around.
- **Second factor (behavioral, named honestly):** operator's Codex sessions have mostly run harness-shaped tasks (kickoff, ingestion) where the ritual is the work; Claude sessions have mostly run dev/build work where nothing re-prompts the close-of-turn reflexes. The pointer file therefore carries the compressed session reflexes (open with /flight-plan; raw intake before wiki; T1 → handoff; defects → /log-backlog; close with /sweep), not just the reading order.
- **RESOLVED 2026-07-27 (tag v3.0.11):** root `CLAUDE.md` ships — a pointer, never a copy, the exact AGENTS.md pattern with Claude-Code-specific notes (auto-load behavior; dev-repo .template caveat) plus the session-reflexes block. `TEMPLATE-README.md` canonical-paths table gains the row. Fork counterpart same session.
- **References:** operator observation 2026-07-27; v3.0-50 (the mirror-image defect); root `AGENTS.md` (the pattern this completes).
- **Status:** CLOSED 2026-07-27 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-58 — Backlog number collisions are structural: the shipped backlog carries the template's numbering cursor, then template and every instance increment `v3.0-N` independently — first live collision demonstrated
- **Surfaced:** 2026-07-27, by the v1.1-instance upgrade session's adoption report: upstream `v3.0-51`/`v3.0-52` (compile:false audit gap; REVIEW staleness) collide with that instance's own local `v3.0-51`/`v3.0-52` (shipped-non-empty backlog; `placeholder_scan_exempt` gap) — same numbers, different content. The instance's local v3.0-51 had *predicted this exact collision*. Root cause is two by-design facts composing badly: (a) `harness-backlog.md` ships non-empty (deliberate — `/log-backlog` appends to it and `/doctor` FIX lines cite entry numbers), so the template's entries AND its numbering cursor travel into every instance; (b) `v<template_version>-N` increments independently in every repo sharing the `3.0` prefix. Collisions aren't a risk; they're a schedule.
- **Interim convention (declared now, instance-side):** instance-local entries use `v3.0-local-N`. Where a collision already exists, the INSTANCE side renumbers with a provenance line ("renumbered from v3.0-NN on <date> to resolve collision with upstream; original number recorded here") — numbers-are-permanent presumes a single namespace, and a collision breaks that premise; the template side never renumbers (its numbers are cited by shipped doctor FIX lines).
- **Proposed durable fix (deferred, design pass):** `/log-backlog` numbering becomes instance-namespaced (e.g. `<project_slug>-N` read from project.yaml) at the next minor; shipped-entry citations (doctor FIX lines) must remain resolvable; MIGRATION recipe for existing instances.
- **References:** the upgrade session's adoption report 2026-07-27; MIGRATION.md #10 (numbering discipline); v3.0-24-era decision to ship the backlog non-empty.
- **Status:** OPEN

### v3.0-59 — Instances can't answer "check for updates to the harness": no shipped surface carries the template's public address, and no instance-side record tracks which release is actually adopted
- **Surfaced:** 2026-07-27, operator question ("is the public repo path written into the harness, so someone could just say 'check for updates'?"). Verified: zero hits for the public repo URL across every instance-bound surface (README, TEMPLATE-README, AGENTS.md, root CLAUDE.md, project.yaml.example, schema, contract template). `template_version` stays `"3.0"` on patch adoption by design (MIGRATION provenance rule), so an instance holds neither the address nor its own patch-level. Every adoption this week worked only because the operator hand-carried URL+tag in prompts — the operator is the fleet's DNS and version registry, which is the bottleneck principle violated in miniature and the reason the "check for updates" ask cannot be self-serve.
- **Proposed fix (deferred to next patch, small):** (a) `project.yaml` gains two fields — `template_source:` (the public repo URL, stamped by init from the template it ran from) and `template_release:` (the tag actually instantiated, e.g. `v3.0.11`; adoption sessions advance it in the same commit as the adoption — the instance-side sibling of the fleet ledger's same-breath rule, and MIGRATION's provenance rule is untouched: `template_version` still tracks the major/minor contract, `template_release` tracks the patch level actually running); schema + example updated; existing instances backfill via one-line adoption-prompt step. (b) An update check — report-only, offline-tolerant: `git ls-remote --tags <template_source>` compared against `template_release`; newer tags = "updates available: vX..vY, adopt file-level per the standard prompt — never git pull". Home: a `/doctor` check (skip-not-fail when offline) or an `/orient`-answerable recipe; decide at build. (c) The standard adoption-prompt template becomes a shipped doc so "check for updates → here's the adoption worklist" is one motion.
- **RESOLVED 2026-07-27 (tag v3.0.12):** all three slices shipped — (a) `template_source` + `template_release` fields (project.yaml.example + schema; both init scripts stamp them append-if-absent, release read from the new root `RELEASE` file the export now carries — the artifact finally knows its own tag); (b) `core/governance/check-template-updates.py` (core, ships always; report-only ls-remote compare; exit 1 = updates-available signal; offline = INCONCLUSIVE never silent-green; self-test 7/7 offline-safe) wired as `/sweep` step 12; (c) `core/onboarding/UPDATING.md` — the adoption method as a shipped doc (diff-driven worklist, plain-vs-substituted file rules, never-copy-backlog, advance-template_release-same-commit, pre-v3.0.12 backfill, the v3.0-58 numbering note). "Check for updates to the harness" is now a self-serve sentence on any current instance.
- **References:** operator question 2026-07-27; this week's five hand-carried adoption prompts; MIGRATION.md provenance rule; FLEET-LEDGER.md (the operator-side half of the same record).
- **Status:** CLOSED 2026-07-27 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-55 — Enforcement without enumeration: the template's sensors replay promises no manifest ever wrote down (69/69 green over an unpressured denominator — the doctrine's own named failure mode, self-applied)
- **Surfaced:** 2026-07-27, operator-requested step-back reasoning run applying manifest-driven-builds doctrine at scale to the harness itself. The week's five defects (v3.0-49..54: generation-lane depth, Claude-only wiring, unverified compile:false, unlifecycled REVIEW, undeclared folder homes) were all DENOMINATOR DISCOVERIES — template promises that existed unenumerated, found by incident (instance once-overs, one operator question), each answered with a bespoke sensor. Every sensor is a replay mechanism in MDD's own terms; every green count (doctor 69/69, parity 85/85, knowledge-debt 7/7) is rows-replayed-over-rows-replayed. MDD §10: "100% coverage over an unpressured denominator is the new QA-passed." The template preaches this for product builds and does not apply it to itself. Recurrence pattern named: v3.0-24 was the same lesson at the DOCS layer (fixed by the W1 truth wave + W5 self-sweep); this is the CONVENTIONS layer; the FLEET layer is next (see v3.0-56). Self-application decays between waves; only an enumerated denominator shows the decay before an incident does.
- **Proposed fix (passes MDD's own admission rule — named cost: five entries in one week; row schema: assertion + bound sensor; replay: sensors already exist):** a TREE-CONTRACT manifest for the template, seeded RETROACTIVELY from what is already built — every doctor check, every sensor assertion, and backlog entries 49–54 become the initial rows; each row binds to its replay sensor or is marked UNSENSED. The UNSENSED count is the template's new headline honesty number. Lands inside existing machinery (check-manifest validation, manifest-format row discipline) and folds into the open v3.0-44 dogfood lane — a dimension, not a new layer, per §4's admission rule. DEFERRED, named: inverted coverage over sensors (every sensor must discharge a row — the check-derivation orphan class); twin-INSTANTIATION divergence hunt (stand the template up twice from the zip, diff through the contract — the v3.0.4 smoke test came one instance short of inventing this).
- **References:** step-back run 2026-07-27; core/methodology/manifest-driven-builds.md §4 (admission rule), §10 (coverage honesty, twin-build); v3.0-24 (the docs-layer recurrence); v3.0-44 (the open dogfood lane this folds into).
- **Status:** OPEN

### v3.0-56 — The fleet has no manifest: N instances × unknown versions × unknown patch adoption × unknown health, with the operator as the only cross-instance record
- **Surfaced:** 2026-07-27, same step-back run, second break-template finding: "the template is the product" is false at scale — the FLEET is (six-plus instances spanning template versions 1.1 through 3.0-current; the named roster lives in FLEET-LEDGER.md, dev-only by operator requirement). Every template promise multiplies by N instances; every defect this week surfaced FLEET-side; the week's instance once-overs were manual conformance replays that no row anywhere counts. The only record of who-runs-what-with-which-patches is the operator's memory plus chat transcripts — the bottleneck principle violated one level up.
- **Proposed fix (deferred, cheap first slice):** a FLEET LEDGER, one operator-facing file in the template dev repo — instance × template_version × patches adopted (AGENTS.md yes/no, v3.0.6–8 yes/no, ...) × last-known doctor/sweep state × date — maintained manually at first (each adoption prompt updates its row), sensor-assisted later (gh-API version probe is mechanical). First consumer: the pending public-repo export of v3.0.6–3.0.8, which currently has no record of which instances still need which patches. DEFERRED, named: per-instance conformance replay on cadence; an adoption channel with receipts.
- **first slice RESOLVED 2026-07-27 (with v3.0.10):** `FLEET-LEDGER.md` created in the dev repo — 8 instance rows populated from live gh probes (template_version + AGENTS.md presence verified per repo, not remembered; found one v1.3 instance independently invented its own AGENTS.md months before 3.0.7 shipped one — the v3.0-50 class self-confirming), adoption-debt priority list, update-in-the-same-breath rule stated in the file. Export-excluded BY NAME from birth (MAINTENANCE.md command + verification invariants; operator requirement 2026-07-27 — the portfolio map never ships). **Residuals open:** per-instance conformance replay on cadence; adoption channel with receipts; gh-probe automation.
- **References:** step-back run 2026-07-27; this week's adoption prompts (three instances — each a hand-carried patch with no ledger row until now); MAINTENANCE.md export ritual.
- **Status:** OPEN

### v3.0-60 — template_release has no honest value for a deliberately-partial adoption: the update check can't distinguish "behind" from "behind on purpose"
- **Surfaced:** 2026-07-27, first field use of the v3.0.12 update machinery: a v3.0-GA-based instance scoped its adoption to the sanctioned subset + core skills, deferring the memory-engine deploy refresh to its own session. Its scope gate proposed stamping `template_release: "v3.0.12"` after the partial adoption — which would make check-template-updates report "up to date" forever over a deliberately incomplete surface (the declared-but-unverified class again, v3.0-51's family). Operator ruling: partial adoptions stamp the honest BASE tag and enumerate the adopted subset in the commit; the resulting permanent "updates available" nag is true and serves as the standing reminder — but it is a nag, and it erases the real information (which subset is current).
- **Proposed fix (deferred, design pass):** a machine-readable adoption-scope record the sensor can read — e.g. `template_adoptions:` list in project.yaml (tag + scope note per adoption) or a dated adoption ledger file; check-template-updates then reports "partial: base v3.0, sanctioned-subset current to v3.0.12, engine deferred" instead of a bare behind/ahead. Must not weaken the honesty direction: absence of a scope record = plain behind.
- **References:** the instance's scope-gate question 2026-07-27 (screenshot, operator-relayed); UPDATING.md (whose method assumed full adoption); v3.0-59 (the machinery this sharpens).
- **Status:** OPEN

### v3.0-61 — First-field-contact defects from the v3.0.12 adoption wave: validator walks git-excluded worktree debris (92 false failures), counts a binary grep line as a placeholder, UPDATING.md fails the scanner it teaches adopters to satisfy; update-sensor renders "v3.0" as "v3.0.0"
- **Surfaced:** 2026-07-27, the first two field adoptions of the v3.0.12 machinery (both instance sessions filed these as local entries with proposed upstream fixes — the local→upstream lane working as designed). (1) init-validate walks `.claude/worktrees/` — tool-managed scratch checkouts, git-excluded via `.git/info/exclude` — producing 92 false placeholder failures on one live instance; (2) the sh scanner's plain grep reports "Binary file … matches" as an unresolved placeholder on a tracked PNG; (3) `core/onboarding/UPDATING.md` line ~37 contained a literal double-brace marker in prose and isn't exempt-listed — the file that teaches adoption failed the validator on the adoption that installs it; (4) cosmetic: check-template-updates normalizes the operator's recorded "v3.0" to "v3.0.0" in output.
- **RESOLVED 2026-07-27 (tag v3.0.13):** (1) both validators exclude `.claude/worktrees/`; (2) sh grep gains `-I`, ps1 gains a binary-extension skip; (3) UPDATING.md describes the markers in words, no literal braces (chosen over exempt-listing — an exemption could hide a genuine unresolved placeholder in that file forever); (4) sensor displays the recorded string verbatim. Sensor self-test 7/7 unchanged.
- **References:** the two instance adoption reports 2026-07-27 (each carrying these as v3.0-local entries); v3.0-58 (the local-numbering convention those reports correctly used).
- **Status:** CLOSED 2026-07-27 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-62 — Mechanized stranger test: instantiation-surface changes ship on precedent-citing skip lines instead of a forced fresh-eyes run
- **Surfaced:** 2026-08-04, silence-and-ambiguity sweep (dev `audits/2026-08-04-silence-sweep.md`) — the audit itself is this entry's evidence. Releases v3.0.14 through v3.0.23 each shipped with "stranger test not re-run this patch (unchanged instantiation surface; precedent …)" — a self-graded judgment call with no mechanical check behind it. Meanwhile the audit found ~130 verified findings, a third of them in exactly the surfaces the stranger test exists to exercise (init scripts, validators, skill wiring, sensor empty-states on a fresh tree). The "unchanged surface" claim was never derived from the diff; it was asserted from memory — the same self-belief class G3 closed for identity stamps.
- **Where:** make-release.py (the natural refusal home), init.sh/init.ps1, init-validate.*, core/skills/, capabilities/knowledge-os/extracted/deploy/
- **Severity:** standard (the audit is the recurrence evidence: silent instantiation-surface rot survives release after release)
- **Symptom:** a release whose diff touches init, validators, shipped skills, or sensors can ship without any fresh-instantiation run; the skip line cites precedent, and nothing checks the precedent's premise.
- **Proposed fix (designed build, not a tonight patch):** a diff-classifier in make-release.py — classify every path in `git diff <last-tag>..<tag>` against a declared instantiation-surface set (init scripts, validators, core/skills/**, extracted skill + sensor sources, project.yaml.schema). If any surface path changed, the export REFUSES unless a stranger-test receipt exists for this tag (a dated artifact recording: fresh instantiation from the candidate zip into a scratch dir, init + validator + doctor + each sensor's live run against the fresh tree, outcomes recorded). The receipt is evidence of a run, never a checkbox — same doctrine as the standing-loop rehearsal receipt (v3.0.23 G1). Skip stays possible ONLY for diffs that touch no surface path, and then the skip line is machine-derived, not remembered.
- **References:** audits/2026-08-04-silence-sweep.md (remediation item 5); HARNESS-CHANGELOG.md v3.0.14–v3.0.23 skip lines; v3.0.23 G1 (receipt-without-run = fabrication doctrine).
- **Status:** OPEN

### v3.0-63 — Plan-scoped totality: the verify packet never carries the plan's claim routing, so verifiers grade every view against total coverage and reject in 0/N waves
- **Surfaced:** 2026-08-04 sweep, from two live instances' independent verify-rejection storms: LAMPS 2026-08-03 (14/14 legs rejected, cross-view leakage + totality dominant) and Ultrapak 2026-08-05 (verify-signal noise attributed to "the two upstream packet defects already logged"). The compile plan ROUTES each event's claims across views deliberately (Step 2's routing judgment, batch caps, pending_cascade), but the verify packet hands the verifier only the event + one view — so a correctly-narrowed view reads to the verifier as an omission, and a whole run's legs reject together (0/N), drowning real defects in structural noise. The per-view totality doctrine (absorb doctrine (i), OPERATIONS.md §6) and the plan's routing are both right; the packet just never tells the verifier which claims THIS view owns.
- **Where:** capabilities/knowledge-os/extracted/deploy/compile-v2.py (packet assembly), compile-backends.py (manifest claim fields), engine/OPERATIONS.md §7, compile/SKILL.md.template Step 2/3a
- **Severity:** standard (recurring on every multi-view routing; each wave costs paid cross-vendor legs and operator adjudication time, and trains sessions to distrust verdicts — the exact pressure that produced the self-adjudication incident v3.0.22 closed)
- **Symptom:** wide-source events fanned across several views produce coordinated rejected verdicts citing missing claims that were deliberately routed to sibling views; the run's true defects are buried in the wave.
- **Proposed fix (designed build):** the plan records per-view claim routing (which of the event's load-bearing claims this view owns; which are declared routed-elsewhere with their target views); emit_packets carries that routing into the manifest and the verify packet; the verifier's charge changes from "is every claim of the event represented" to two graded questions — per-view fidelity to DECLARED scope (owned claims all present and faithful) and run-level union coverage (every claim owned by exactly one view in the run, or named in pending_cascade). A view faithful to its declared scope confirms even when siblings carry the rest; a claim NO view owns is the run-level failure it always was. No gate weakens: the union check is new mechanical coverage the current per-view grading only approximates by noise.
- **References:** audits/2026-08-04-silence-sweep.md (remediation item 5); HARNESS-CHANGELOG.md v3.0.20 (the first rejection wave's lessons); v3.0.22 (no-self-adjudication — this entry removes the noise that invited it); LAMPS v3.0-local-5; Ultrapak session report 2026-08-05.
- **RESOLVED 2026-08-06 (tag v3.0.29):** plan-scoped totality as designed (operator-approved design session same day, folding v3.0-67/68 + the set-aside baseline amendment). The plan's optional per-source `claim_routing` (each claim owned by exactly one view or deferred with named targets) is validated mechanically pre-write (`check_claim_routing`, run() + emit_packets — a claim owned by nobody refuses the run, full stop), journaled on the compile record, carried into absorb packets (DECLARED CLAIM SCOPE) and verify packets (DECLARED CLAIM ROUTING), and the verifier's charge becomes the two graded questions (owned-claim fidelity to declared scope; enumeration completeness, reason class `enumeration-incomplete`) — one verdict per leg, enum untouched. All-legs-enumeration-incomplete doctrine: ONE plan defect, never N article defects (OPERATIONS.md §7). Deferred claims ride the driver summary's Deferred band → receipt `pending_cascade.claims_deferred` (shape single-homed in wiki-schema §7). Legacy plans/records byte-identical. Acceptance fixture (all four cases demonstrated in compile-v2 --self-test): narrowed view CONFIRMS / missing owned claim REJECTS / unowned claim refuses pre-write / update with real baseline + narrowed scope CONFIRMS. Batteries: compile-v2 175/175, compile-backends 176/176, compile-driver 172/172.
- **Status:** CLOSED 2026-08-06 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-64 — Project-state snapshot: every observer skill embeds its own copy of "run the sensor battery," so the battery runs twice-plus per session and every new skill re-creates the duplication
- **Surfaced:** 2026-08-05, plain-language sweep (dev `audits/2026-08-05-plain-language-sweep.md`, Theme 6) + the same session's skill-architecture reasoning run (operator-endorsed). The mandated session-open sequence runs doctor + six sensors twice (sweep then flight-plan; flight-plan's dedupe keys on a briefing FILE a manual sweep is forbidden to write); check-derivation --gate runs twice per sweep (doctor check 9 + sweep step 4); check-manifest twice when a smoke set exists; doctor re-proves 63 sensor self-tests per run. The sweep's twelve pairwise dedupe fixes (S1-S12) are prose threads between specific steps — symptomatic, drift-prone, and blind to future skills.
- **Where:** capabilities/knowledge-os/extracted/deploy/ (sensor family), core/skills/doctor/doctor.py, core/skills/sweep/SKILL.md, core/skills/flight-plan/SKILL.md.template, capabilities/knowledge-os/extracted/audit/SKILL.md.template
- **Severity:** standard (fixed minutes of duplicated wall-clock at every session open — a named component of the operator's "40 minutes is unacceptable" complaint — plus a structural recurrence guarantee for every future observer skill)
- **Symptom:** the same read-only checks execute multiple times per session over an unchanged tree; sessions feel slow; skill prose accumulates hand-sewn "read step X's result" threads that drift.
- **Proposed fix (designed build):** one sensor-battery runner that writes a project-state snapshot artifact stamped with the tree state it observed (git HEAD + dirty-file digest — freshness is mechanical, never a 24-hour guess). Every skill's sensor step becomes "read the snapshot if its stamp matches the current tree, else refresh it." Trust checks stay honest: what a snapshot-served PASS attests (which battery, which tree-state, when it last ran live) is printed, never silently narrowed. Subsumes the plain-language sweep's interim speed dedupes S1-S5/S12, which retire when this lands. Companion of v3.0-65 — the input side of the same kernel (observe once → tell once).
- **References:** audits/2026-08-05-plain-language-sweep.md (Theme 6; plan item 1(e); skill-architecture reasoning run in the execution log); LAMPS 40-minute compile session 2026-08-03 (specimen 1).
- **Status:** OPEN

### v3.0-65 — Structured operator-report renderer: operator voice is authored per-skill in sixteen places, so plain language is a promise where it should be a mechanism
- **Surfaced:** 2026-08-05, plain-language sweep — 56 consolidated findings, of which Theme 1 (no report contract), Theme 2 (the decision surface), and Theme 4 (sensor voice) are all per-skill copies of one function: telling the operator. The harness's single enforced voice contract (sweep briefing + check-briefing-format.py) proves the mechanism works; it is wired to one surface of many. The v3.0.25 fix single-homes the contract TEXT and points validators at more surfaces — but sessions still hand-compose every report, and every new skill adds another authoring site.
- **Where:** core/governance/CLAUDE.md.template (§ Reporting, once shipped), capabilities/knowledge-os/extracted/deploy/decision-inbox.py (the existing proto-renderer), deploy/check-briefing-format.py, every SKILL.md report step
- **Severity:** nice-to-have (v3.0.25's contract + enforcement covers the acute defect; this entry is the structural end-state)
- **Symptom:** report quality depends on each session honoring prose instructions; voice drifts per skill; a rewrite of the contract means re-touching every skill's report step.
- **Proposed fix (designed build):** skills and sensors emit structured findings (what / consequence / owner / offered-action / details-ref — the three-sentence form as a schema, which v3.0.25 already converges all surfaces toward); one renderer produces SESSION-BRIEFING, SWEEP-BRIEFING, DECISIONS-PENDING, and the chat-report skeletons from that structure, with the voice contract enforced at the single render site. decision-inbox.py is the seed (it already scans sources and renders one surface); this generalizes it. Companion of v3.0-64 — the output side of the same kernel.
- **References:** audits/2026-08-05-plain-language-sweep.md (Theme 1 lesson, plan item 1(e), both reasoning runs); manifests/sweep-briefing/format-MANIFEST.md (the proof-of-concept contract).
- **Status:** OPEN

### v3.0-66 — Two v3.0.25/26 tools contradict each other: the decision inbox's own machine header trips the prose scan the sweep runs over it
- **Surfaced:** 2026-08-05, Ultrapak's v3.0.25-27 adoption session (instance-logged as v3.0-local-4, correctly local-prefixed) — the first live sweep after adopting flagged DECISIONS-PENDING.md's derived-projection HTML header (`<!-- ... regenerated by deploy/decision-inbox.py ... -->`) under the new `no-script-names-in-prose` scan (/sweep step 16, shipped v3.0.25). Reproduced upstream same-day against the v3.0.27 tree: `decision-inbox.py` writes the header by contract (`di-header-comment` row, EXACT), the scan reads raw bytes — a false positive on every instance, every sweep, forever.
- **Where:** capabilities/knowledge-os/extracted/deploy/check-briefing-format.py (`prose_only`), the sweep step-16 wiring
- **Severity:** standard (a permanent false positive in the operator's briefing is exactly the alarm-fatigue class the sweep exists to kill)
- **Patched (dev, same-day):** `prose_only()` now strips HTML comments before the leakage/script-name rows — a comment never renders, so it is not prose the operator reads; a script name in VISIBLE text beside a comment still fails (self-test 17→19/19, both cases pinned). Ships as v3.0.28 on the operator's word.
- **Proposed fix:** as patched; instance-side, adopting v3.0.28's re-copy of the validator clears the standing Watching item.
- **References:** Ultrapak adoption report 2026-08-05 (v3.0-local-4); manifests/decision-inbox/format-MANIFEST.md row `di-header-comment`; audits/2026-08-05-plain-language-sweep.md (release 1, item (d)).
- **Status:** CLOSED 2026-08-05 — fixed as the whole of release v3.0.28 (check-briefing-format's prose scan strips HTML comments; self-test 19/19); the entry predates the fix note.

### v3.0-67 — Verify packet hands the checker an EMPTY pre-absorb baseline for updates to existing views, so every update-diff reads as created-from-nothing
- **Surfaced:** 2026-08-05, Ultrapak's first post-v3.0.28 compile (instance-logged with mechanical proof): all three verify legs declined to confirm with the SAME structural reason — each checker separately judged the absorption faithful to its routed events and faulted only the comparison it was given ("the shown diff creates the entire 122-line view from an empty file"; "the most important missing artifact is the actual previously verified view baseline"). The instance session verified mechanically that the packet's before-file was git's canonical empty-blob marker while the real pre-absorb articles were substantial and previously verified. Distinct from v3.0-63 (claim routing): 63 is the checker not knowing which claims a view owns; THIS is the checker not being shown what the view looked like before. Both corrupt the same packet, and both produce the 0/N waves that train sessions toward the self-adjudication the rules forbid. This one survived v3.0.25-28 because none of those releases touched packet assembly.
- **Where:** capabilities/knowledge-os/extracted/deploy/compile-v2.py / compile-backends.py (verify-packet diff construction — wherever the pre-absorb baseline is resolved), engine/OPERATIONS.md §7
- **Severity:** blocking-in-effect for update-heavy instances (every compile that UPDATES existing articles ends at the gate on structural grounds; only creates-from-nothing runs can confirm — backwards)
- **Proposed fix:** the verify packet's "before" must be the view's real pre-absorb content (the previously verified baseline the journal/run commit already knows); a genuinely NEW view legitimately verifies from empty and says so in the packet. Fold into the v3.0-63 build session — same files, same packet surface, one acceptance fixture covers both: an UPDATE to an existing view with correct baseline + narrowed scope must CONFIRM.
- **References:** Ultrapak compile escalation report 2026-08-05 (operator ruling recorded instance-side); harness-backlog v3.0-63; audits/2026-08-05-plain-language-sweep.md (the "verify-noise class" boundary note — this defect is exactly why the sweep only fixed the telling).
- **RESOLVED 2026-08-06 (tag v3.0.29, folded into the v3.0-63 build):** root cause found upstream — the verify pass picked its never-verified diff base from the FIRST absorption anywhere in journal history, INCLUDING reverted runs, so a rejected-then-reverted creation's empty blob poisoned every later verify of that view. Fixed at the baseline-selection rule (`_absorption_trigger_state`/`_cumulative_diff`): reverted runs' ghosts excluded; base = last machine-verified state, else last operator-adjudicated state (new `--set-aside` driver mode journals the ruling and advances the baseline — operator amendment: a set-aside ALSO advances, a bare rejection never does), else the view's real pre-absorb content. Every packet's diff section now NAMES its baseline in words; a genuinely NEW view says "verifies from empty" out loud. Acceptance case (d) demonstrated: an update to an existing view with a reverted creation ghost in history gets its real substantial baseline and CONFIRMS with narrowed scope.
- **Status:** CLOSED 2026-08-06 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-68 — Compile's per-view author fan-out never assigns per-agent scratch namespaces; two parallel authors chose the same scratch filename and clobbered each other
- **Surfaced:** 2026-08-05, Ultrapak's first post-ruling compile (recorded in that run's receipt): three authoring subagents ran in parallel per Step 3's mandatory isolation; two independently chose the same scratch filename and overwrote each other's work. No content lost — the orchestrator caught it and completed the affected views directly (which is itself the leak-prone serial degradation the isolation exists to avoid). The skill mandates WHO authors in isolation but is silent on WHERE: nothing assigns each agent a distinct scratch path, so two literal readers invented the same plausible name — the silence-rule failure shape, in miniature, inside the skill that was rebuilt for isolation.
- **Where:** capabilities/knowledge-os/extracted/compile/SKILL.md.template Step 3 (fan-out instructions)
- **Severity:** standard (silent-overwrite class; today's catch was luck plus a careful orchestrator, and the fallback it forced re-creates the cross-view-leak setup)
- **Proposed fix:** one sentence in Step 3 — each authoring agent works only under its own item-numbered scratch path (e.g. `<staging>/scratch/NN-<slug>/`, mirroring the answer-file naming emit_packets already assigns), and the orchestrator never gives two agents the same directory. Prose-only; no engine change.
- **References:** Ultrapak compile receipt 2026-08-05 (the collision record); compile SKILL Step 2.5/3 (per-view isolation, 2026-08-03 rework).
- **RESOLVED 2026-08-06 (tag v3.0.29, prose rider on the v3.0-63 build):** Step 3 fan-out gains the WHERE: each authoring agent works only under its own item-numbered scratch path (`<staging>/scratch/NN-<slug>/`, mirroring emit_packets' answer-file naming) and the orchestrator never gives two agents the same directory. Prose-only, as proposed.
- **Status:** CLOSED 2026-08-06 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-69 — The engine never mints a derivation region, so an engine-born project can never record a verification
- **Surfaced:** 2026-08-05, Ultrapak — its first-ever confirmed absorption could not be recorded. Verified upstream: compile-v2's absorb write path only STRIPS or WRITES INTO a derivation region, never creates one; every region-writing site in that file is a self-test fixture that hand-authors a view already carrying one (which is why the batteries never caught it); the only real minter is backfill-derivation.py, scoped by its own docstring to legacy hand-era views. A project born on the engine therefore has no article that can ever reach stamped/confirmed state. Two things hid it: the region is schema-optional, and check-derivation --gate looks for audit-pending views, not for missing regions — so /doctor reports green on a wiki where verification is impossible.
- **Where:** capabilities/knowledge-os/extracted/deploy/compile-v2.py (absorb create path), check-derivation.py (the blind spot), backfill-derivation.py (scope note)
- **Severity:** blocking-in-effect (silent, fleet-wide: the verify chain's last link is missing on every project that never migrated from a hand-era corpus)
- **MECHANICALLY REPRODUCED UPSTREAM 2026-08-06** (scratch probe against the v3.0.29 tree, during that release's approval hold): a NEW view absorbed with the `new_text` a real author produces (frontmatter + body, no region — the ANSWER CONTRACT never asks for one) was graded by a verify backend returning **`confirmed`** with a complete F17 substrate block; the engine recorded **zero** confirmations and reported the leg as a non-confirm with `stamp refused: cannot stamp verified: view has no derivation region`. So the failure is not merely "cannot stamp" — a paid cross-vendor approval is produced and then discarded. Confirmed the two blind spots by reading: `check-derivation.stale_verified_check` returns SV_SKIP ("not in scope") when a region is absent, and `audit_pending_t1` returns False without a region, so neither the sensor nor its `--gate` can see a region-less view at all. The v3.0-84 per-leg reason line is what keeps this honest today — it prints "stamp refused", never a fabricated verifier rejection — but the run still ends exit 1 with the article unverified.
- **Interaction with v3.0.29 (why this is the same chain's last link):** v3.0-63/67 make the checker ABLE to confirm a correctly-narrowed or updated article; this entry is why the confirmation cannot be RECORDED on an engine-born wiki. v3.0.29's acceptance fixture holds only for views that already carry a region (its own new-view case hand-authors one — the very blind spot named above); a region-less new-view case is owed to the fix's battery.
- **Proposed fix (v3.0.30):** the absorb path mints a minimal engine-managed region when creating a NEW view, using backfill's conservative defaults (never `legacy-assumed` for an engine-born view — that would assign a migration content-audit obligation the view never earned); check-derivation gains a region-presence check so the blind spot closes; backfill stays the migration path for existing corpora.
- **Interim, no code:** run `backfill-derivation.py --root .` once per instance (verified fit-for-purpose 2026-08-06: it mints for EVERY region-less wiki view, not only legacy ones, skips regenerated projections, is idempotent, validates each mint through check-frontmatter and reverts any file that regresses). Two honest caveats: run it on a worktree or branch, never directly on a live tree (its own SAFETY note); and it stamps engine-born views `consumed_status: legacy-assumed`, which the v3.0.30 mint avoids.
- **References:** Ultrapak session report 2026-08-05; backlog v3.0-63/v3.0-67 (this is the same chain's last link — those make the checker able to confirm, this makes the confirmation recordable); backfill-derivation.py's own docstring, which states the gap verbatim while scoping itself to legacy views.
- **RESOLVED 2026-08-06 (tag v3.0.29, folded in on the operator's word):** the absorb path mints the engine-managed region for views it CREATES — after `validate_absorb_output` passes and before the write, so the author's manifest-vs-diff contract is graded against exactly what the author wrote and the journal's `post_blob` pins the minted text that actually lands. Shape comes from `backfill-derivation.py`'s `render_region` (called, never re-implemented — the two minters cannot drift); `consumed_status: legacy-assumed` chosen deliberately over the alternatives (`audit-pending` would trip check-derivation's F12 gate on every engine-born view forever; `verified-consumed` would assert a verification that has not happened yet, since the verify leg runs after this write — the residual label wart is filed as v3.0-71). Unmintable shapes (regenerated projections, no parseable frontmatter) are left untouched with NO new refusal. `check-derivation` gains the region-presence check — reported always, refusing under `--gate`, remedy named in the output — closing the blind spot where every existing check keyed on a region's contents. Acceptance: the mechanical reproduction promoted into the battery (same input that recorded 0 confirmations against a `confirmed` verdict now records 1, stamped and journaled); verified the minted region introduces zero new check-frontmatter findings. compile-v2 175→191/191, check-derivation 14→18/18.
- **Status:** CLOSED 2026-08-06 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-71 — Nothing ever transitions `consumed_status`, so an engine-born view is permanently labelled "legacy-assumed"
- **Surfaced:** 2026-08-06, while building v3.0-69's region mint. `consumed_status` has exactly three legal values (`verified-consumed`, `legacy-assumed`, `audit-pending`) and NO code path ever moves a view between them: `_stamp_verified_block` rewrites the `verified:` block and leaves every other top-level key byte-untouched by contract, and `backfill-derivation.py` only ever mints. So the value a view is born with is the value it dies with, regardless of how many cross-vendor confirms it earns.
- **Where:** capabilities/knowledge-os/extracted/deploy/compile-v2.py (`_stamp_verified_block`, `_mint_derivation_region`), assemble.py (`gate_view`, the consumer that refuses T1 serving on a non-`verified-consumed` view)
- **Severity:** nice-to-have (honesty/labelling, not a broken gate — the verification itself is recorded correctly in `verified:`, which is what the verify chain actually reads)
- **Symptom:** every engine-born view carries `consumed_status: legacy-assumed` — semantically false (it is not legacy; it was born through the validated absorb path) — and stays there after confirming. Its practical effect is assemble's conservative refusal to serve such a view for T1 build, which is safe-but-wrong. Also affects every backfilled legacy view, which stays `legacy-assumed` forever after being verified.
- **Why it was NOT fixed in v3.0.29:** the alternatives at mint time are all worse (see v3.0-69's resolution), and the correct fix is a state TRANSITION, which is new machinery. A naive "flip to verified-consumed on confirm" is a **loosening**: it would auto-clear the migration content-audit obligation on legacy backfilled views, which is precisely what that obligation exists to hold open.
- **Proposed fix (designed build):** distinguish the two populations — a view minted by the engine (born validated, no migration debt) from a view minted by backfill (carries the F13 audit obligation) — by recording the mint's provenance in the region itself, then allow a confirmed absorption-verify to advance ONLY the engine-born population to `verified-consumed`. The legacy population's obligation stays until an actual content audit clears it, unchanged.
- **References:** v3.0-69 (this residue is named in its resolution); assemble.py's gate doctrine (`legacy-assumed` on T1 → REFUSE for build); check-frontmatter's `consumed_status` enum (the three legal values).
- **Status:** OPEN

### v3.0-72 — A structurally-induced rejection has no disposition, so sessions offer "degrade the article" as a peer option and re-ask the operator every run
- **Surfaced:** 2026-08-06, LAMPS, on an article rejected three times running. The session's adjudication menu offered as an equal third choice "revert it and re-author the article's content to satisfy the checker" — annotated with its OWN finding that this "costs you correct content" and that the three sessions which examined the verdict each found the checker's grounds false. The operator's response, verbatim: *"this is getting old,. why is this an option"*. The menu was not a session defect: it is a faithful rendering of the only vocabulary the doctrine gives.
- **Where:** capabilities/knowledge-os/extracted/engine/OPERATIONS.md §7 (the two-disposition rule), compile/SKILL.md.template Step 3b (the exit-1 escalation spec)
- **Severity:** standard, recurrence-class — each occurrence converts a harness defect into an operator adjudication, and re-converts it on every subsequent run of the same article
- **Symptom:** §7 defines exactly two dispositions — correct through the cycle, or escalate for an operator set-aside — and BOTH presuppose the verdict is right. When a rejection is structurally induced (the packet asked a malformed question; the article is correct as scoped), neither fits. "Correct through the cycle" means degrading accurate content to satisfy a misinformed checker — the knowledge base made worse to turn a gate green. "Set aside" records *the checker erred*, which is a different and false fact when the checker was simply not told what the run covered. Lacking any word for the real case, a literal session enumerates the two it has and prices the damage instead of refusing it.
- **Second defect, same root:** nothing parks the article against its known cause, so it re-enters the adjudication queue every run and the operator is asked to rule on the same harness defect repeatedly. Three times on this article so far.
- **Proposed fix (designed build):** (1) a THIRD disposition in §7 — **park against a named upstream defect**: the article stays absorbed-and-unconfirmed, the verdict and the backlog entry id are recorded together, and the article is suppressed from re-adjudication until that entry resolves, at which point it re-verifies for real. This is not a set-aside (no ruling that the checker erred, so no false fact recorded) and not a correction (no content change); the backlog entry becomes the thing that owns the article's unverified state, instead of the operator owning it once per run. (2) **No session may offer an option its own analysis says costs correct content** — that is a refusal with the reason named, never a priced menu item. A menu listing corpus damage beside honest choices trains the operator to weigh them as peers, and they are not peers. The three-times-rejected article is the acceptance fixture: under the fix it is parked once, against a named entry, and never asked about again until that entry closes.
- **References:** LAMPS adjudication menu 2026-08-06 (both questions, operator-escalated); v3.0.22 no-self-adjudication (this is the same pressure arriving from the other side — that rule stopped sessions from ruling verdicts void, and left the genuinely-misfiring case with nowhere to go); v3.0-63 (same root one level up: a checker graded against a scope the packet never told it); the pending within-one-article scoping entry (LAMPS `revenue-lines`, verdict reason sentences owed).
- **Status:** OPEN

### v3.0-73 — `--revert` assumes the rejected run is still the last word on its articles; on a project that kept working it collides and can jam every future compile
- **Surfaced:** 2026-08-06, Ultrapak's v3.0.29 adoption (instance-logged as v3.0-local-10, **blocking**). The re-ride recipe v3.0.29 shipped tells the session to `--revert --seq N` before re-absorbing. That assumes the rejected run is still the newest thing to have touched its articles. On this instance it was not: normal work had modified all three articles the same day, including splitting one. The only prescribed action therefore collides — and the failure is worse than a refusal.
- **Where:** capabilities/knowledge-os/extracted/deploy/compile-driver.py (`execute_revert` / `revert_run_commit`), MIGRATION.md's v3.0.28→v3.0.29 recipe step 3, compile/SKILL.md.template Step 3b correction cycle
- **Severity:** blocking (the step shipped to FIX re-rides can jam a project that simply kept working — and the recipe pushes every adopting instance straight into it)
- **REPRODUCED UPSTREAM 2026-08-06** (scratch probe): a rejected run, then an ordinary commit editing the same article, then the prescribed `--revert` → `git revert` conflicts. The conflict path aborts and journals `revert-failed`, which is NON-TERMINAL, so startup reconciliation refuses every later run until a human untangles it. The probe surfaced a second, adjacent failure on the same path: the revert-failure record could itself fail to journal (`prev_record_hash mismatch`), leaving the operator a doubled git error and **no record at all**. Which tail fires depends on repo state; both are bad, and both come from attempting the revert before checking whether it can apply.
- **RESOLVED 2026-08-06 (ships next release):** a collision pre-check in `execute_revert`, before anything is written — blob comparison of every non-journal path the run commit wrote against HEAD, fail-closed on any path git cannot resolve on either side. On collision it refuses, names the files that moved on, writes nothing (no commit, no journal record, no blocking state, clean worktree), and documents **the aged case**: correct FORWARD rather than rewinding — take each article's current text as the base, fix what the verdict named in a fresh plan and staging dir, and `--run` that; the rejection stays on the record as history, because it did happen. `--revert` still works normally on a run that IS still the last word (pinned as its own case). Battery 172→176/176.
- **References:** Ultrapak v3.0.29 adoption report 2026-08-06; the v3.0.29 MIGRATION recipe step 3 and both instance re-ride prompts, which contain the trap verbatim and need amending.
- **Status:** CLOSED 2026-08-06 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-74 — A verification that was discarded before v3.0.29 cannot be recovered after it: every door is shut
- **Surfaced:** 2026-08-06, Ultrapak (instance-logged as v3.0-local-8, standard). v3.0-69 stopped approvals being thrown away; nothing recovers one already thrown away. Their workbook article has a genuine second-vendor approval on file that it cannot display.
- **Where:** capabilities/knowledge-os/extracted/deploy/compile-driver.py (`verify_record_legs`, `execute_revert`, `execute_reverify`)
- **Severity:** standard (bounded to articles absorbed before adopting v3.0.29 — but every engine-born instance has them)
- **Symptom:** the run's verdict artifact says `confirmed`, but the pre-fix engine could not stamp it (no derivation region), so the leg landed in `absorption_verify_attempts` with no `absorption_verified` entry. `verify_record_legs` then classifies that leg from the ARTIFACT's value rather than from what the engine actually recorded, so the run reads as fully confirmed: `--reverify` declines ("already has a terminal verify disposition"), `--revert` declines ("fully confirmed -- nothing to adjudicate"), and hand-stamping is forbidden. The article stays permanently unverified while holding a real approval.
- **Root cause worth naming separately:** `absorption_verify_attempts` means, by construction, "this leg produced no stamp". Re-deriving its status from the verdict file contradicts the engine's own record. The two sources disagree and the artifact currently wins.
- **Proposed fix:** judge a run by what the engine RECORDED — a view is confirmed iff it has an `absorption_verified` entry — so a discarded-approval run is correctly seen as not-confirmed and the existing paths reopen. Preferred over the instance's alternative (a narrow re-stamp mode that dispatches nothing), which would add a second stamping path beside `_stamp_verified_block`; but that alternative stays on the table if the classification change proves to have reach beyond this case.
- **References:** Ultrapak report 2026-08-06; v3.0-69 (the defect this is the residue of); v3.0-84 (the per-leg reason line that correctly distinguishes "stamp refused" from a verifier rejection — the information needed for the fix is already recorded).
- **Status:** OPEN

### v3.0-75 — The region-presence check flags hand-authored flight plans and points at a tool that refuses them
- **Surfaced:** 2026-08-06, Ultrapak (instance-logged as v3.0-local-9, nice-to-have). The v3.0.29 sensor listed both of the project's flight plans as articles that can never record a verification, with a FIX line naming `backfill-derivation.py` — which then declines them.
- **Where:** capabilities/knowledge-os/extracted/deploy/check-derivation.py (the v3.0-69 region-presence check)
- **Severity:** nice-to-have (a permanent false positive on every sweep — the alarm-fatigue class, in a check added one release earlier to close a blind spot)
- **Symptom:** `wiki/flight-plans/*.md` are hand-written and direct-editable by design (the single-writer rule's documented exception in `core/governance/CLAUDE.md`), never absorbed through the engine. They have nothing to verify and no region to carry. The check excluded regenerated projections by BASENAME and never considered whole non-compiled subtrees.
- **RESOLVED 2026-08-06 (ships next release):** the check skips non-compiled subtrees (`NON_COMPILED_DIRS`, currently `flight-plans/`) alongside the projection basenames, with the reasoning recorded at the constant. Battery 18→19/19.
- **References:** Ultrapak report 2026-08-06; v3.0-69 (the check this corrects); CLAUDE.md single-writer rule (why flight plans are exempt by design, not by accident).
- **Status:** CLOSED 2026-08-06 — resolution recorded in the entry's own RESOLVED note above.

### v3.0-76 — The `RELEASE` marker survives init as a second version-bearing file that nothing reads and no recipe updates
- **Surfaced:** 2026-08-06, Ultrapak (instance-logged as v3.0-local-11, nice-to-have) — found three releases behind the project's real version and synced by hand. Previously predicted: the 2026-08-04 silence sweep filed this as finding **I9** and it never got its own entry.
- **Where:** init.sh / init.ps1 (which delete `VERSION` but not `RELEASE`), MIGRATION.md (no recipe names it), `project.yaml.template_version` (the actual single home)
- **Severity:** nice-to-have (a stale version marker that misleads any reader who trusts it, including a future session orienting itself)
- **Symptom:** init deletes `VERSION` citing single-source doctrine but leaves `RELEASE`, which is a template-repo artifact. Post-adoption it is read by nothing, updated by no recipe, and drifts permanently — while `project.yaml.template_version` is the real home.
- **Proposed fix:** init deletes `RELEASE` alongside `VERSION`, same doctrine and same line of code; MIGRATION gains a one-line note for instances that already carry a stale copy (delete it; your version lives in `project.yaml`). Decide upstream whether `RELEASE` should ship in the published mirror at all, since the dev repo is where the release ritual reads it.
- **References:** Ultrapak report 2026-08-06; audits/2026-08-04-silence-sweep.md finding I9 (predicted, unfiled); v3.0.24 cluster #10 (the same single-home discipline applied to step counts).
- **Status:** CLOSED 2026-08-08 — init.sh/init.ps1 now consume RELEASE into project.yaml.template_release (same doctrine as VERSION); init-validate check 8b refuses a leftover; existing instances delete their stale copy per MIGRATION.

### v3.0-70 — The shipped-state guard matches "live" as a bare substring, so it rejects "deliverable", "delivery", and any heading merely containing those letters
- **Surfaced:** twice now — LAMPS 2026-08-03 (two headings renamed to get past it) and again 2026-08-05 on the round-4 provenance compile. The guard exists to stop absorbed prose from asserting shipped-state; on a project with no execution corpus there is no shipped-state to assert, so it only blocks legitimate English ("live lane", "deliverable dates").
- **Where:** the absorb-output validator's shipped-state check, `compile-v2.py` `validate_absorb_output` (the REFUSING site) and the same expression in `reconcile_flags` (the FLAGGING site — same false positives, lower blast radius)
- **Severity:** standard (recurring friction; forces authors to reword accurate prose)
- **VERIFIED UPSTREAM 2026-08-06** (against the v3.0.29 tree, during that release's approval hold): the expression is `re.search(r"(shipped|live|as-built)", section, re.I)` with no word boundaries, so it fires on every heading merely CONTAINING those letters. Reproduced across a heading set: `Deliverable Dates`, `Delivery Schedule`, `Deliverables`, `Olive oil sourcing`, `Lively debate`, `Livelihood` — all matched on `live`; only `Planning lane` came back clean. The reported LAMPS cases are two instances of a defect with a much wider mouth than the report shows.
- **The fix splits into two halves, and only one is free:**
  - **(a) The substring bug — pure defect, no judgment.** Anchoring to `\b(shipped|live|as-built)\b` clears `deliverable`, `delivery`, `olive`, `lively`, and `livelihood` while still matching `Shipped state`, `As-built notes`, and `Currently live in production` — verified across the same heading set. Zero coverage lost; this is a false-positive removal, not a loosening.
  - **(b) `Live lane` — a real judgment call, operator's to make.** That heading uses "live" as an ordinary adjective and still matches under word boundaries. Closing it means either gating the guard on a declared corpus binding (the entry's first proposal) or moving from heading-substring matching to assertion-shape matching over the added prose. **Note against the first proposal:** the guard is SATISFIABLE on a corpus-less project — `corpus_support` accepts any sha-pinned repo file with exact quoted lines, including a raw event — so switching it off where `corpus_source` is undeclared removes a gate that currently works rather than one that cannot be met. Under the standing "never loosen a gate" rule that half needs an explicit operator decision; half (a) does not.
- **Proposed fix:** ship (a) as the mechanical correction at both sites; hold (b) for an operator ruling on which of the two shapes the guard should take.
- **PARTIALLY RESOLVED 2026-08-06 (tag v3.0.29) — half (a) only:** the expression is now word-anchored and SINGLE-HOMED as `_SHIPPED_STATE_RE`, replacing the byte-identical copies at the refusing site (`validate_absorb_output`) and the flagging site (`reconcile_flags`). `Deliverable Dates`, `Delivery Schedule`, `Deliverables`, `Olive oil sourcing`, `Lively debate` and `Livelihood` all pass now; `Shipped state`, `As-built notes` and `Currently live in production` still refuse without `corpus_support` — both directions pinned in compile-v2's battery, so no coverage was traded for the fix.
- **STILL OPEN — half (b), awaiting an operator ruling:** a heading using "live" as an ordinary adjective (`Live lane`) still trips the guard. Closing it means either gating the guard on a declared corpus binding or matching assertion shapes over the added prose instead of the heading. The corpus-binding option is a LOOSENING and should be weighed as one: the guard is satisfiable on a corpus-less project, because `corpus_support` accepts any sha-pinned repo file with exact quoted lines, including a raw event. So the question is not "make an unmeetable gate meetable" but "should this gate exist where no corpus is declared" — an operator call under the never-loosen rule, not a patch.
- **References:** LAMPS session reports 2026-08-03 and 2026-08-05; compile-v2.py `validate_absorb_output` + `reconcile_flags`; the ANSWER CONTRACT's "shipped-state / live / as-built prose REQUIRES corpus_support" line, which is the doctrine both sites enforce.
- **Status:** OPEN


### v3.0-91 — Trajectory-replay battery: extend drill-golden-replay to replay verify-verdict lifecycles
- **Surfaced:** 2026-08-08, structural-audit remediation session (Phase 4 filing; operator-directed follow-up).
- **Where:** `capabilities/knowledge-os/extracted/deploy/drill-golden-replay.py` (the read-only full-history accounting replay); the verify-verdict lifecycle paths in `compile-v2.py`/`compile-driver.py` (`absorption_verify_attempts`, `absorption_verified`, `absorption_adjudicated`, `--revert`/`--set-aside`/`--reverify`).
- **Severity:** standard (dev-only tooling; the lifecycle is the engine's only history-dependent surface and its defect record — v3.0-67/69/73/74 — shows state-machine bugs survive the per-function batteries).
- **Symptom:** the existing batteries test lifecycle transitions case-by-case; nothing replays a real project's full verify-verdict TRAJECTORY (create → reject → revert/set-aside → update → confirm across many runs) against the journal to prove the recorded states stay reachable and consistent end-to-end. v3.0-73 (a revert colliding with later ordinary work) and v3.0-74 (a leg with no recoverable stamp) are both trajectory bugs, invisible to single-step tests.
- **Proposed fix:** a dev-only trajectory-replay battery extending `drill-golden-replay.py`: replay journal histories (real + synthetic multi-run fixtures) through the lifecycle classifier and assert every leg lands in exactly one recorded terminal state with its baseline derivable. DEV-ONLY (rides the make-release DEV_ONLY_DEPLOY exclusion), and built only AFTER the Phase 3 verifier-demotion design settles — the demotion changes the state table this battery would pin.
- **References:** 2026-08-08 structural audit; v3.0-73/74; HARNESS-CHANGELOG v3.0.29/v3.0.30.
- **Status:** OPEN

### v3.0-92 — OPERATIONS.md §7: replace the accreted verify-verdict prose with one state table
- **Surfaced:** 2026-08-08, structural-audit remediation session (Phase 4 filing; operator-directed follow-up).
- **Where:** `capabilities/knowledge-os/extracted/engine/OPERATIONS.md` §7 (cross-vendor verify) and its failure-handling cross-references.
- **Severity:** standard (the section has grown by accretion across v3.0.22/25/29/30 — no-self-adjudication, set-aside, revert-vs-forward, scoped totality, escalation contract — and now states the verdict lifecycle as layered prose amendments rather than one authoritative table).
- **Symptom:** a session answering "what happens to a rejected leg in state X" must reconcile four releases' worth of amendments; the audit's many-homed-facts mechanism predicts drift here next.
- **Proposed fix:** rewrite §7 around a single state table (state × event → next state, journal record, who may act), with the prose demoted to rationale. DIFF-REVIEWED by the operator, and rides a future release that touches §7 anyway (first candidate: the Phase 3 verifier-demotion build, which must restate §7's blocking/recorded split) — never its own churn release.
- **References:** 2026-08-08 structural audit; HARNESS-CHANGELOG v3.0.22–v3.0.30 §7-touching entries.
- **Status:** CLOSED 2026-08-09 — rode the verifier-demotion build (v3.0.32) as its pre-authorized rider, separate commit: §7 now opens with the eight-state lifecycle table as the stated authority (every v3.0.22/25/29/30 + demotion amendment traceable to a row); the prose demoted to a rationale section, no doctrine sentence deleted (citations preserved).

### v3.0-93 — register-candidates.py self-test fails 22/26 on Windows (case F write-failure simulation)
- **Surfaced:** 2026-08-08, structural-audit remediation session — noticed in passing during the Phase 2 scratch init run (doctor's python-sensors check reported it on a fresh instantiation; reproduced directly in the dev tree, same 22/26).
- **Where:** `capabilities/knowledge-os/extracted/deploy/register-candidates.py` (self-test case group F: "a write failure after 1 success stops immediately and reports").
- **Severity:** standard (a fresh instance's first doctor run opens with a FAIL the operator cannot act on; the live append path may be fine — the failing cases are the failure-simulation legs).
- **Symptom:** `python deploy/register-candidates.py --self-test` exits 1 with 22/26, cases (F) failing; doctor renders it NEEDS-YOU on day one — the exact alarm-fatigue class v3.0.26 killed elsewhere.
- **Proposed fix:** diagnose the case-F failure on Windows (likely the write-failure simulation's permission/locking mechanism not failing as staged under NTFS); fix the simulation or the code under test; pin both directions. Verify on a clean instantiation.
- **References:** this session's scratch init runs (both HEAD and working tree — pre-existing, not introduced here).
- **Status:** OPEN

### v3.0-94 — compile-receipt manifest's docs/wiki-schema.md pin is unverifiable in the dev tree and stale since v3.0.29
- **Surfaced:** 2026-08-08, structural-audit remediation session — noticed while re-hashing the sweep-briefing manifest's source pin (Phase 1's A3 amendment).
- **Where:** `capabilities/knowledge-os/extracted/manifests/compile-receipt/format-MANIFEST.md` frontmatter (`- path: docs/wiki-schema.md`, sha256 `d3d76d21...`); `check-manifest.py`'s sha256-pins check.
- **Severity:** standard (a pin that nothing can verify is the audit's phantom-authority shape wearing a checksum).
- **Symptom:** in the dev tree the pinned path resolves to nothing (`check-manifest` SKIPs it "absent (may be by design)"), so releases that edit `wiki-schema.md.template` — v3.0.29 did, and this remediation did — cannot know they staled the pin; on an instance the rendered `docs/wiki-schema.md` contains substituted values, so the recorded hash may match no tree anywhere. The v3.0.27 "re-hashed" note on this surface is unreproducible from the shipped bytes.
- **Proposed fix:** decide what the pin means (hash of the template file, resolved dual-layout like the sweep-briefing pin resolves `.claude/skills/` → `core/skills/`; or drop the pin for a section-reference), then make check-manifest verify it in BOTH layouts so a stale pin fails in dev, not on an instance.
- **References:** this session's Phase 1 (sweep-briefing pin re-hash precedent); v3.0.27 release note (d); v3.0.29 wiki-schema edits.
- **Status:** OPEN

### v3.0-95 — The egress hook is deny-only, so authorized network work dead-ends into "disable the hook and restart"
- **Surfaced:** 2026-08-09, operator report from a live instance session (a Replicate API probe): the PowerShell-egress pattern (`block-dangerous-bash.sh` line 20) blocked an operator-authorized read-only API call, and the only remedies the refusal message names are "temporarily disable the hook or whitelist the specific command" — the second of which is a mechanism the hook does not have. Operator, verbatim: *"These things have come up quite a few times and prevented me from doing real work."*
- **Where:** `core/security/hooks/block-dangerous-bash.sh` (`DENY_PATTERNS`, one exit-2 verdict for everything), the PreToolUse wiring in `settings.local.json.example.template`
- **Severity:** standard, recurrence-class — every legitimate egress call on every instance hits it; the working fallback (operator runs the command in their own shell) routes real work around the harness, and the tempting fix (comment out one line + restart) drops the entire egress perimeter for the whole session.
- **Symptom:** the hook has exactly one verdict — hard deny — for two different risk classes. Destructive commands (`rm -rf /`, `git reset --hard`, root-targeting `Remove-Item`) are unrecoverable, so deny is right. Network egress is reviewable-before-run, but the hook cannot express "ask the operator," so an authorized call has no path that keeps the perimeter on. This contradicts the harness's own deny-list doctrine (v3.0.19: deny ONLY the unrecoverable or secret-touching) — applied to the permission deny-list in that release and never to the hook layer.
- **Proposed fix (operator-gated — it changes the security perimeter):** split the patterns into two tiers. **DENY** (unchanged, exit 2): the destructive family. **ASK**: the egress family (curl/wget/nc/netcat, the PowerShell egress cmdlets and aliases, inline interpreter `-c`/`-e`) returns Claude Code's PreToolUse `"ask"` permission decision instead — the operator sees the exact command and approves or declines that one call; in an unattended session nobody answers, so an unanswered ask fails closed and the perimeter holds exactly where it matters most. Optional third rail for standing workflows: a project-local allowlist of exact command prefixes, each row carrying a recorded operator grant (the HUMAN-GATE pattern), consulted before the ask tier. Either way the refusal/ask message must name a remedy that actually exists.
- **References:** v3.0.19 (the deny-list doctrine this extends to hooks); `core/security/CREDENTIALS.md` (secret egress stays excluded regardless of tier); the 2026-08-09 instance transcript (operator-supplied).
- **Status:** OPEN
